# P0.10 — Ancre référence (GateKind export + meal-first label)

**Verdict : GO**

Lot **P0.10** = observation Autodrive gate, un seul PR, **le booléen `engage` et les doses ne bougent pas**.
Relecture du code (study / HEAD #84 / commits source). Rien n’est inventé depuis le texte du PR.

| Rôle | SHA | Branche / objet |
|---|---|---|
| Study post-P0.9 (base #84) | `172654f2619c0ee131452062ab4c6154e472233b` | `kmp-aimi-migration-study` après merge #82 |
| HEAD P0.10 | `c1776c5d3731f47a4214f7e458be7774777407f4` | PR **#84** `cursor/p0-10-autodrive-gatekind-f0c2` |
| Source GateKind + export | `078a51514c76cc1a0750f4ee26e2ee950b614457` | `feat(aimi): say why the Autodrive gate stayed shut` |
| Source meal-first label | `0ca774418419cfc6c6ba5b6c8e823075438dd436` | `fix(aimi): let a meal keep its name above the plateau line` |
| Tip contexte `dev_OAPSAIMI` | `fe96b64f12f2f691ec6cfeeebf5c39bd446bedbc` | **pas** d’autres commits post-gel (`f519`, replay, DescentRedoseGuard, …) |

`078a51514c` ⊂ `0ca7744184` ⊂ `fe96b64f12`. Diff `AutoDriveGater.kt` **vide** entre `0ca7744184` et `fe96b64f12` : la ref P0.10 pour le gater / `GateKind` = `0ca7` = tip. L’export `baseline_state` est déjà dans `078a` ; `0ca7` ne le retouche pas.

---

## 1. Contenu réel de PR #84

- **1 commit** : `c1776c5d37` — *P0.10 — Autodrive GateKind export + meal-first label*
- **4 fichiers**, +324 / −7
- Base GitHub : `kmp-aimi-migration-study` @ `172654f261`
- Parent du commit = `172654f261` (aucun autre commit study)

| Fichier study | Statut |
|---|---|
| `plugins/aps/src/androidMain/.../DetermineBasalAIMI2.kt` | modifié (+25) |
| `plugins/aps/src/androidMain/.../autodrive/safety/AutoDriveGater.kt` → `commonMain/.../AutoDriveGater.kt` | **renommé** (R059) + GateKind / meal-first |
| `commonTest/.../AutoDriveGaterKindTest.kt` | ajouté |
| `commonTest/.../AutoDriveGaterEngageKindOrderTest.kt` | ajouté |

Pas d’autre fichier. Pas de `OpenAPSAIMIPlugin`, pas d’`AutodriveEngine`, pas d’`InsulinActionModel`, pas de dump staging, pas d’Advisor, pas de `DescentRedoseGuard`, pas de plancher IAM `f519`.

---

## 2. Tableau des 2 items

| # | Item | Statut | Fidélité vs `078a` / `0ca7` | Preuve |
|---|---|---|---|---|
| 1 | `GateKind` + export (`078a51514c`) | **Appliqué** | Enum 6 valeurs **identique** (`HR_HIGH`, `ACTIVITY`, `RISE_TOO_WEAK`, `HIGH_PLATEAU`, `MEAL_AWARE_RISE`, `STRONG_RISE`). `GatingResult.kind` défaut `RISE_TOO_WEAK`. Trois champs écrits **avant** `if (!gate.engage)`. | Study `AutoDriveGater.kt` L127–165 ≡ `0ca7` (KDoc meal-first inclus). Export study `DetermineBasalAIMI2.kt` L537–541 / L861–863 / L5377–5381 = `078a` L529–533 / L891–893 / L5410–5412 (`AimiJson.NULL` vs `JSONObject.NULL`). |
| 2 | Libellé repas en premier (`0ca7744184`) | **Appliqué** | `engageKind` : `isMealRising` **avant** `isHighPlateau`. `engage` reste `isHighPlateau \|\| isActivelyRising \|\| isMealRising`. Seule la cellule plateau+repas change de nom (`HIGH_PLATEAU` → `MEAL_AWARE_RISE`). | Study L93–106 ≡ `0ca7` L92–105 (alignement whitespace près). Grille 8 cellules + 6 tests portés (`AutoDriveGaterEngageKindOrderTest`). |

`shouldEngageV3` : signature **inchangée** (9 params, mêmes défauts). Call site production **identique** à la base `172654f261` et à `078a` (glucose, `combinedDelta`, COB, UAM, meal flags, lookback 75 m, Ra, `mealChannelHint`).

---

## 3. Fidélité clinique (observation only)

### 3.1 `engage` — OR inchangé

Formule study `172654f261` = HEAD `c1776c5d37` = `078a` = `0ca7` = `fe96` :

```
hardHRBlock     = hrNow >= 140
activityBlock   = bg < 160 && stepsLast15m > 1000 && stepsLast5m > 0
isHighPlateau   = bg > 150
isActivelyRising = (bg≥150 ∧ Δ>0.8) ∨ (bg≥120 ∧ (Δ>1.2 ∨ (Δ>0.8 ∧ (UAM≥0.5 ∨ Ra≥0.7)))) ∨ (bg<120 ∧ Δ>2.0 ∧ minBg75 ≥ REBOUND)
isMealRising    = implicitMealContext ∧ Δ > 0.25
shouldEngage    = isHighPlateau || isActivelyRising || isMealRising
```

`implicitMealContext` identique : `!SUPPRESS && (PRIORITY || explicitMealMode || hasRecentMealEstimate || cob>8 || (cob>3 && UAM≥0.35))`.

Le `when` meal-first **ne décide pas** l’ouverture. `DetermineBasalAIMI2` branche toujours uniquement sur `gate.engage` (L5384 / L5393) — mêmes deux `if` que `172654f261` L5359 / L5368.

### 3.2 `GateKind` + export

- Sorties bloquantes : `kind = HR_HIGH` ou `ACTIVITY` (même ternaire que `078a`).
- Disengage glycémique : `kind = RISE_TOO_WEAK`.
- Engage : `kind = engageKind` (meal-first depuis `0ca7`).
- Écriture **avant** la branche, deux issues. Clés : `autodrive_gate_engaged`, `autodrive_gate_kind` (`gate.kind.name`), `autodrive_gate_reason` (`gate.reason`).
- Lecteurs `autodrive_gate_*` / `gate.kind` sur `c1776c5d37` : **uniquement** ces trois écritures + l’export JSON + les 2 tests. Rien côté dose / `AutodriveEngine` / barrière / SMB.
- `gate.reason` sur le chemin engagé : **même** ligne debug qu’avant (`🚦 [AUTODRIVE V3] ${gate.reason}`).

Phrase KDoc study en plus sur les 3 champs : *« Observation only. Nothing dose-facing reads these three fields. »* — pas dans `078a`. Hors sémantique.

### 3.3 Meal-first — label, pas dose

Avant `0ca7` / avant P0.10, `engageReason` lisait le plateau d’abord (`172654f261` L92–96 = `078a` L92–96). Au-dessus de BG 150 un repas était nommé `HIGH_PLATEAU`. Après : `MEAL_AWARE_RISE` gagne si les deux sont vrais. Les 7 autres cellules de la grille gardent le même `kind`. Lock : `only the plateau-and-meal tick changed its name`.

### 3.4 Ancre dose

Aucun move. Pas de `profileAnchoredSafetySi`, pas de `profile.sens`, pas de `enforce`, pas de `safeU`. Le diff `DetermineBasalAIMI2` ne contient aucun symbole de dose hors le commentaire « Nothing dose-facing… » et le voisinage `effort_smb_*` déjà présent (non modifié).

---

## 4. Hors-scope respecté

| Interdit P0.10 | Constat @ `c1776c5d37` |
|---|---|
| P0.11 IAM floor `f519320841` | Hors PR. |
| P0.12 barrier replay `79588b42cb` | Hors PR. |
| P1.1 SMB `smbGiven` | Hors PR. |
| P1.2 `DescentRedoseGuard` `fe96b64f12` | Hors PR. |
| Advisor ZIP / dashboard UI / `81b401a7f9` timeline | Aucun fichier UI / Advisor / staging. |
| Remplacement `DetermineBasalAIMI2` / `OpenAPSAIMIPlugin` / `AutodriveEngine` | Non. +25 lignes d’export dans le tick ; plugin / engine absents du diff. |
| Changement de `shouldEngage` / seuils / meal context | Non. OR + seuils byte-identiques à `172654f261`. |

Entre `078a` et `fe96` : `f519`, `79588`, `81b401`, `0ca7`, `fe96`. Seuls `078a` + `0ca7` sont dans #84.

⚠️ ASYNC IMPACT : aucun. Pas de nouvelle coroutine. `shouldEngageV3` reste synchrone ; signature inchangée.

---

## 5. Adaptations KMP (câblage) vs divergences sémantiques

| Sujet | Adaptation KMP | Divergence sémantique ? |
|---|---|---|
| Source set gater | `androidMain` → `commonMain` (R059). Imports déjà ports + Metro + `CorrectionAggressionGate` ; **aucun** import Android. | Non. Placement. FQCN inchangé. |
| Santé | Study `AimiHealthContext` (port common, préexistant). Ref `HealthContextRepository` (javax). | Non. Même `fetchSnapshotForAutodriveGater()`. |
| Metro | `@Inject` / `@SingleIn(AppScope)` conservés (ref = `@Singleton` / javax). Constructeur inchangé. | Non. Préexistant P0.x. |
| Export JSON | `AimiJson.NULL` (ref `JSONObject.NULL`). Mêmes 3 clés, mêmes types, écriture avant branche. | Non. |
| Tests | `commonTest` + `kotlin.test` + `IdleHealth` / `NoOpAapsLogger` (ref `src/test` + Truth + mockito/JUnit5). | Non. Mêmes 3+6 locks, même grille (bg 180/110, Δ 1.5/0.3/2.5, `explicitMealMode`, `PRIORITY` / `SUPPRESS`). |
| Noms backtick | Virgules retirées (`e34b01f666`, Kotlin/Native). Ex. `every kind is distinct so counting…`. | Non. Assertions identiques. |

**Aucune divergence sémantique** sur les 2 items.

---

## 6. Caveats vs blockers

### Blockers

Aucun. Pas de change de `engage`, pas de change de dose, pas de hors-scope, pas d’écart de formule.

### Caveats (suivre, ne bloquent pas le merge P0.10)

1. **`HR_HIGH` / `ACTIVITY` hors grille** — identique à `078a` (l’enum / le défaut sont lockés ; la santé idle empêche ces branches). Pas un manque P0.10.
2. **`kind` n’a qu’un consommateur** — l’export `baseline_state`. `engageReason` ne va qu’à une ligne debug. Voulu par `078a` / `0ca7`.
3. **CI GitHub #84** — `ios` fail + `claude-review` fail. Même forme que #80 / #82 (préexistant, hors lot). Ne juge pas ce diff.
4. **Preuves de build** — le corps du PR cite `compileAndroidMain`, `compileKotlinIosArm64`, `jvmTest` Kind 3 + Order 6 = 9/9, et un red/green meal-first (`expected: MEAL_AWARE_RISE but was: HIGH_PLATEAU`, même message que `0ca7`). Cette ancre **relit le code** ; elle ne relance pas Gradle.
5. **Move `commonMain`** — placement KMP annoncé, pas un changement de contrat Metro. À garder en tête pour les lots suivants (le gater compile désormais Native).

---

## 7. Checklist preuves

- [x] PR #84 : 1 commit `c1776c5d37`, 4 fichiers, base / parent `172654f261`
- [x] `078a51514c` ⊂ `0ca7744184` ⊂ `fe96b64f12` ; diff gater `0ca7`↔`fe96` vide
- [x] `GateKind` 6 valeurs + défaut `RISE_TOO_WEAK` study ≡ `0ca7`
- [x] Export 3 clés avant `if (!gate.engage)` ; `AimiJson` vs `JSONObject` seulement
- [x] `shouldEngage` OR + seuils + meal context study ≡ `172654f261` ≡ `078a` ≡ `0ca7`
- [x] `engageKind` meal-first study ≡ `0ca7` (plus le plateau-first de `078a` / de la base study)
- [x] Call site `shouldEngageV3(...)` inchangé ; branche production = `gate.engage` seulement
- [x] Aucun lecteur dose-facing de `autodrive_gate_*` / `gate.kind`
- [x] Aucun fichier hors lot ; Metro / `AimiJson` / signature `shouldEngageV3` conservés
- [x] 2 tests portés (locks équivalents, `commonTest`, noms sans virgule)

---

## 8. Recommandation

| Action | Reco |
|---|---|
| **Merge #84** | **Oui** — observation only, fidèle à `078a` puis `0ca7`, hors-scope tenu. |
| **Fix avant merge** | **Non** — pas de blocker clinique. |
| **Suite** | P0.11 IAM floor `f519` (dose-facing, **replay**). Ne pas greffer barrier replay / `smbGiven` / `DescentRedoseGuard` / Advisor dans le même PR. |

P0.9 (#82) avait clos les leftovers d’observation Autodrive `db213`. P0.10 ajoute l’explication du gate (`kind` + export) et lève le masque de label repas-sous-plateau. Ce n’est pas un changement de décision.
