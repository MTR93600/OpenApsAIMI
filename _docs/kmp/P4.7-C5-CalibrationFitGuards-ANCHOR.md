# P4.7 — ancre C5, garde-fous du fit de calibration

Une seule droite : `fitLinearCalibration`. `calibrate()` et `status()` lisent cette droite et la même décision d’appliquer ou non (`isApplicable`, ou les quatre prédicats dans le même ordre). Pas de second calcul.

## Tips lus au démarrage

| Rôle | SHA | Sujet |
|---|---|---|
| Study `kmp-aimi-migration-study` | `e91fc46d3e5caa715a43cf7b3a0256519007b99e` | `P4.6 — calibration status() / CalibrationStatus (#131)` |
| Ref `origin/dev_OAPSAIMI` | `6598201d26e0bc95524e1c5edb1af9a538f1654e` | `feat(dashboard): implement TIR tracking and display with historical comparison` |

Les deux tips sont ceux attendus (`e91fc46d`, `6598201d`). `git fetch origin dev_OAPSAIMI` n’a pas bougé le tip.

Le commit source nommé par le lot est `1b81e356c839ef30a3f78a2737c6e7733d434dfe`. Entre ce commit et le tip, **un** commit touche encore `CalibrationMath.kt` et `CalibrationMathTest.kt` : `c653fc4485dd985088d9a30a42c99af4e3b285e4` (`feat(calibration): add new strings for calibration feedback and warnings`). Il est déjà dans `6598201d`. Ce qu’il change sur le fit, et que ce lot porte :

- suppression de `CORRECTION_AT_CENTER_MIN`
- `CORRECTION_AT_LOW_MIN = -35`
- `correctionInRange` ne borne plus que le haut
- `lowEndSafe` devient un intervalle `[CORRECTION_AT_LOW_MIN, CORRECTION_AT_LOW_MAX]`, plus seulement un plafond

Le tip n’a pas bougé. La calibration, elle, a bougé après `1b81e356`. Le portage est celui du tip, pas l’état intermédiaire de `1b81e356` seul.

Lignes « ref » : `plugins/calibration/src/main/kotlin/.../CalibrationMath.kt` @ `6598201d`. Lignes « study » : `plugins/calibration/src/commonMain/kotlin/.../CalibrationMath.kt` après ce lot, sauf mention.

## Tableau règle / ref / study

| Règle | Ref @ `6598201d` | Study après ce lot | Verdict |
|---|---|---|---|
| Moins de 3 entrées : pas de pente | `MIN_ENTRIES_FOR_SLOPE = 3`, L87. Usage L192 : `entries.size < MIN_ENTRIES_FOR_SLOPE \|\| sensorRange < MIN_SENSOR_RANGE_FOR_SLOPE` → `OffsetOnly`, pente 1, offset = moyenne pondérée des écarts | L97 et L206, même condition, même branche offset-only déjà écrite | **≡** |
| Plancher d’entrées du fit | `MIN_ENTRIES_FOR_FIT = 2`, L75. Moins de 2 → null | L83, inchangé | **≡** |
| Écart capteur pour une pente | `MIN_SENSOR_RANGE_FOR_SLOPE = 54`, L103, dans le même `if` que le nombre d’entrées | L113, même `if` | **≡** |
| Correction en un point | `correctionAt`, L138 : `(slope - 1) * sensorMgdl + offset` | L148 | **≡** |
| Correction au centre | `correctionAt(CENTER_MGDL)`, L135. `CENTER_MGDL = 100`, L20 | L145. `CENTER_MGDL` L20 | **≡** |
| Correction au centre, sens | `correctionInRange`, L155 : `correctionAtCenter <= CORRECTION_AT_CENTER_MAX`. `CORRECTION_AT_CENTER_MAX = 30`, L21. Pas de plancher. `CORRECTION_AT_CENTER_MIN` n’existe plus | L167. La constante basse est retirée | **≡** |
| Bas, les deux sens | `LOW_MGDL = 40`, L32. `CORRECTION_AT_LOW_MAX = 20`, L35. `CORRECTION_AT_LOW_MIN = -35`, L62 (`c653fc4485`). `lowEndSafe`, L161 : `correctionAtLow in CORRECTION_AT_LOW_MIN..CORRECTION_AT_LOW_MAX` | L36, L39, L68, L175 | **≡** |
| Haut | `HIGH_MGDL = 300`, L64. `MAX_RATIO_AT_HIGH = 1.45`, L73. `ratioAtHigh`, L144 : `(slope * HIGH_MGDL + offset) / HIGH_MGDL`. `highEndSafe`, L164 : `ratioAtHigh <= MAX_RATIO_AT_HIGH` | L70, L81, L154, L178 | **≡** |
| Applicable | L166 : `slopeInRange && correctionInRange && lowEndSafe && highEndSafe`. `slopeInRange` L146 : `slope in SLOPE_MIN..SLOPE_MAX` (`0.55`..`1.6`, L14–15) | L180 et L156. `SLOPE_MIN` / `SLOPE_MAX` inchangés | **≡** |
| `calibrate` applique ou non | `LinearCalibrationPlugin.kt` L158 `slopeInRange`, L162 `correctionInRange`, L172 `lowEndSafe`, L183 `highEndSafe`, puis blend. Identité dès qu’un prédicat est faux. Les quatre messages de log sont ceux du tip, dont « mid-range lift … above » (plus le libellé two-sided) | L176, L180, L191, L202, puis blend L212. Même ordre, mêmes chaînes. Pas une seconde droite | **≡** |
| `status()` | L217 `fitLinearCalibration`, L219 `!fit.isApplicable` → `UnsafeFit`, puis le mode. Pas de blend | `calibrationStatus` L31–36. Le plugin `status()` charge les entrées et appelle cette fonction. Il ne refait pas les quatre tests | **≡** |
| `effectiveFit` / santé | fit brut, puis `isApplicable`, puis blend. Santé : `!fit.isApplicable` → alerte unsafe | `effectiveFit` L280–281. Santé L446–452. Le prédicat élargi s’applique tout seul | **≡** sur le prédicat. La santé P3.8 n’est pas réécrite |
| Horloge | `dateUtil.now()` dans le plugin | `calibrate` et `status()` prennent `dateUtil.now()`. Le fit et `calibrationStatus` reçoivent `now`. Pas de `System.currentTimeMillis` | **≡** |

Aucune règle du tip ne dépend d’un symbole absent de study. Le KDoc de `MIN_ENTRIES_FOR_SLOPE` cite Dexcom ONE+ et Libre 3 comme motif : la formule ne les appelle pas. Les drivers restent hors lot.

Le commentaire ref L23–26 décrit la droite géométrique de deux points (pente ≈ 0,571, capteur 55 → environ 104). Ce n’est pas ce que `fitLinearCalibration` renvoie pour ces deux points : L192 force `OffsetOnly`. Study garde le commentaire (il dit pourquoi les extrémités existent) et la phrase qui rappelle que deux points ne produisent pas cette droite. La droite, elle, sort du fit à trois points.

## Avant / après

Poids égaux (horodatage = `now`). « Avant » = study `e91fc46d`. « Après » = ce lot, aligné sur le tip.

Le KDoc de `CORRECTION_AT_LOW_MIN` reste celui de la ref L56–58 : « offset −55,4 » et « 31,4 mg/dL » retirés à une lecture de 40. C’est l’arrondi du commentaire, gardé par fidélité. Les tests du cas pente 1,6 (`calibrate()` et les bornes) partent des valeurs exactes du fit et des tests ref : offset −55,44, correction à 40 = −31,44, capteur 300 → 424,56, ratio 424,56/300 = 1,4152.

| Cas | Avant | Après |
|---|---|---|
| 110→135 et 180→175 | `Full`, pente 4/7, offset 505/7, correction au centre 205/7 ≈ +29,3, `isApplicable` vrai, `status` = `Applied`. Capteur 55 → 725/7 ≈ 103,6 | `OffsetOnly`, pente 1, offset +10 (écarts +25 et −5), `AppliedOffsetOnly`. Capteur 55 → 65 |
| 110→135, 145→155, 180→175 | même droite `Full` (le troisième point est sur la droite). Correction à 40 mg/dL = 385/7 = +55. Le centre passe. `isApplicable` ne regarde pas le bas : `Applied`. `calibrate` applique | même droite. `lowEndSafe` faux (+55 > 20). `UnsafeFit`. `calibrate` renvoie l’identité |
| Droite `209/309`, offset 0 (sang 209, capteur 309) | correction au centre −10000/309 ≈ −32,36, hors `[−30, 30]`, `isApplicable` faux | `correctionInRange` vrai. Correction à 40 = −12,945, dans `[−35, 20]` (le test ref attend −12,96 ± 0,1). `isApplicable` vrai |
| Pente 1, offset −100 | centre −100, déjà refusé par le plancher −30 | le centre ne refuse plus la baisse. Correction à 40 = −100 < −35 : `lowEndSafe` faux, toujours refusé |
| Pente 1,6, offset −55,44 (fit Syai clampé du test ref : 72→54, 122,4→140,4, 172,8→226,8) | centre ≈ +4,56, dans `[−30, 30]`, `isApplicable` vrai. Capteur 300 → 424,56 | correction à 40 = −31,44, dans `[−35, 20]`. Ratio 1,4152 ≤ 1,45. `isApplicable` vrai. `status` = `AppliedSlopeClamped`. `calibrate` écrit 424,56 |
| Trois points 100→129, 150→209, 200→289 (`y = 1,6x − 31`) | `Full`, centre +29 dans `[−30, 30]`, bas sûr, `isApplicable` vrai, `Applied`. Capteur 300 → 449 | ratio 449/300 ≈ 1,497 > 1,45. `highEndSafe` faux. `UnsafeFit`. Identité. Ce n’est pas le fit −55,44 |
| Fixture ref `CalibrationFit(1.6, -30)` (300 → 450, ratio 1,5) | le centre IEEE vaut `30.000000000000007`, déjà hors de l’intervalle fermé `[−30, 30]`. Study refusait déjà. Ce point ne montre pas le changement à lui seul | `highEndSafe` faux. `isApplicable` faux. C’est le cas du test ref, pas l’exemple chiffré du changement |

## Appelants du fit

`fitLinearCalibration` n’est défini qu’une fois (`CalibrationMath.kt` L202).

| Appelant study | Rôle | Décision |
|---|---|---|
| `LinearCalibrationPlugin.calibrate` L171 | applique la droite aux glycémies | les quatre prédicats, ordre ref, puis blend |
| `calibrationStatus` L31, appelé par `LinearCalibrationPlugin.status` | `CalibrationStatus` | `isApplicable`, puis le mode. Pas de blend |
| `effectiveFit` L280 | pente du seuil de delta (`checkPreconditions`) | `isApplicable` sur le fit brut, puis blend |
| `checkCalibrationHealthAndNotify` L446 | alerte | `!isApplicable` → unsafe. Pas une seconde formule |
| `CalibrationViewModel.recomputeSuspend` L84 | écran de gestion | le même fit. Les entrées viennent de `getValidCalibrationEntriesSince(sessionStart)`, pas de `entriesForFit`. Écart déjà là avant ce lot. Non modifié |

`CalibrationScreen.StatusCard` L208 et `CalibrationScatterChart.android.kt` L102 lisent `isApplicable` sur le fit déjà calculé. Ils ne rappellent pas `fitLinearCalibration`.

## Tests adaptés

Ils encodaient l’ancienne pente à deux points, ou le centre borné des deux côtés.

| Test | Ancien verrou | Adaptation |
|---|---|---|
| `CalibrationStatusTest.status_follows_the_reference_order` | 100→200 et 200→400 en `SlopeClamped` | `OffsetOnly`, offset +150, toujours `UnsafeFit` (le centre +150 dépasse 30). Trois points sur `y = 2x` gardent le cas `SlopeClamped` non applicable |
| `CalibrationStatusTest.a_fit_the_reference_rejects_at_the_low_end_stays_applied_until_c5` | trois points, `Applied` | renommé `a_fit_the_reference_rejects_at_the_low_end_is_unsafe`. `UnsafeFit`, et les deux points en `AppliedOffsetOnly` |
| `CalibrationMathTest` (androidHostTest) `wideSpread`, `atThreshold`, `futureDated`, `slopeAboveMax`, `slopeBelowMin`, `negativeDeltas` | deux points, mode `Full` ou `SlopeClamped` | troisième point sur la même droite, comme le test ref. La pente testée a encore trois entrées |
| `CalibrationMathTest.calibrationFit_steepSlopeWithLargeOffset_clampedByCorrectionNotOffset` | `correctionInRange` faux pour centre −83 | comme le ref : le centre ne refuse plus la baisse ; `lowEndSafe` faux et `isApplicable` faux |
| `LinearCalibrationPluginTest.calibrate_goodFitButStale_notifiesToRecalibrate` | deux points périmés → alerte « stale » | troisième point. Deux points seraient `OffsetOnly` et l’alerte deviendrait « narrow range » |
| `LinearCalibrationPluginTest.addEntry_deltaThresholdScaledBySlopeWhenFitApplicable` et `addEntry_deltaExceedsScaledThreshold_rejectsWithScaledThreshold` | deux points, pente 1,05, seuil 5,25 | trois points sur `y = 1,05x`. Deux points donneraient pente 1 et seuil 5,0 |

Non modifiés, assertion encore vraie (le mode change, le test ne le verrouille pas) :

- `calibrate_validFit_appliesSlopeAndOffset` : deux points sur `y = 1,1x`, lus au milieu (150). L’offset-only (+15) donne aussi 165. La ref a laissé cette fixture à deux points.
- `calibrate_slopeOutOfRange`, `status_slopeOutOfRange`, `calibrate_unsafeFit` : deux points `y = 2x` deviennent offset +150, toujours identité / `UnsafeFit`.
- `calibrate_pureOffset`, `twoGoodEntries` (offset +7,5 encore applicable), `calibrationFit_correctionAtCenter` (centre −4, bas −34 ≥ −35, ratio ≈ 1,32).

## Hors lot

Staging Libre 3 (`ignoreEntriesBefore`), drivers (`calibratesInSensor`), `TrainingResult`, shadow meal-boost, `AimiLoopRuntimeGuard`, pompes iOS, santé P3.8 (le prédicat change, le `when` de la santé non), préconditions et trou P4.5b (la sélection `entriesForFit` n’est pas retouchée). Le ViewModel continue de fitter les entrées brutes de la session.
