# Ancre P3.4 — AnticipationBasalFloor + FCL meal basal (aaa305 / bce972 / 41e59f9)

> Date d’ancre : 2026-09-18
> Lot : **P3.4 only** — PR [#107](https://github.com/MTR93600/OpenApsAIMI/pull/107)
> **Verdict : GO_WITH_CAVEATS**
> Aucun changement clinique dans ce document. Relecture du code, pas d’invention.

## SHA

| Rôle | SHA | Preuve |
|---|---|---|
| Study base (base de PR #107) | `28010b205838062b2d73a4b38f0e6f1a3728fcb1` | tip `kmp-aimi-migration-study` post P3.3 (#105) |
| PR #107 head | `1c74717b1dc254ea97c9ab70ce371fc53743baaf` | 2e commit (fix stub commonTest) ; parent de `ff9fa62c64` = study base |
| 1er commit PR | `ff9fa62c645818fd40285adcb84de02b22e332e4` | port Anticipation + FCL |
| Ancre clinique demandée | `aaa30588f7c0c44dc45c889f6a632e63dab827a1` | tip `origin/dev_OAPSAIMI` au brief |
| `origin/dev_OAPSAIMI` au jour de l’ancre | `5f02b1718ea9101beca79f1aacfd7ac2fc753c64` | 1 commit **après** `aaa305` (`feat: sensor age repair` Dexcom One+) |
| Source AnticipationBasalFloor | `bce972221cde95a4d28b2b2695f7b3db9d863edd` | commit qui **ajoute** le geste |
| Blob helper Anticipation | `4cd866d93659a237bc4e670a4bce6acc1aaeae99` | **identique** `bce972` = `aaa305` = tip `5f02` |
| Blob tests Anticipation | `3b82d5722a7cdfaee8c26a15bd78f9365a72b922` | **identique** `bce972` = `aaa305` = tip `5f02` |
| Source FCL (plafond, sans P1) | `3dd116826a472944fbf11600fc3d6ca784e59231` | « meal basal ceiling without prebolus » |
| Source FCL (déclaration + P1) | `41e59f9bc02439112979c31bbb93dc262537d5d6` | ajoute `FCL_P1` + `declared()` |
| Blob helper FCL | `5a9ad2a4b57542f5abeb5c53b1541cd52216f614` | **identique** `41e59f9` = `aaa305` = tip `5f02` |
| Blob tests FCL | `bcdde71e94b1381e7e45a72ebf79b8e7dc8901de` | **identique** `41e59f9` = `aaa305` = tip `5f02` |
| Blob tests Therapy anticip | `770a5f6fae477ded6367285e6ee73eba81f2ce37` | **identique** `bce972` → tip `5f02` |
| Blob tests Therapy FCL | `6a984e58c9f375f1566b7f7192fb6ae14e30fb9d` | **identique** `3dd116` → tip `5f02` |
| Branche PR | `cursor/p34-anticipation-fcl-3c50` | 2 commits, **+1298 / −3**, **15 fichiers** |

`git log --oneline bce972221c..aaa30588f7 -- AnticipationBasalFloor.kt AnticipationBasalFloorTest.kt` : **vide**.
`git log --oneline 41e59f9bc0..aaa30588f7 -- FclMealBasal.kt FclMealBasalTest.kt` : **vide**.
`git diff --name-only aaa30588f7..5f02b1718e` sur chemins cliniques Anticip/FCL/Therapy/BooleanKey/DetermineBasal/DPA : **NONE**.

`FCL_P1` **absent** de `3dd116`, **présent** (3 hits) sur `41e59f9` — le lot study porte le raffinement `41e59f9`, pas le FCL « plafond seul » de `3dd116`.

---

## Verdict

**GO_WITH_CAVEATS** — merger PR #107 sur `kmp-aimi-migration-study`.

Le contrat clinique de `bce972221c` (Anticipation, inchangé sur `aaa30588f7` / `5f02b1718e`) et de `41e59f9bc0` (FCL `declared` + plafond meal + prébolus one-shot `FCL_P1`, inchangé sur le même tip) est porté. Prefs Anticipation **default `false`**. FCL **sans pref** : note `"fcl"` + TT ≤ 85 mg/dL + pas de sport. Call-sites DetermineBasal / plugin / DPA `declaredMeal` **comme le tip**. Helpers purs, formules identiques. Hors-scope P3.5–P3.8 / MCER latch / TPO / ML stale tenu. Aucune formule inventée.

Ne pas classer **GO** : le lot est **dose-facing et mixed**. Anticipation OFF = aucun changement de dose ; ON = **monte** la basale. FCL n’a pas d’opt-in : dès que la personne déclare, ça **monte** basale **et** tire un prébolus Autodrive. Review serrée, même classe que P3.2 / P3.3, plus le geste qui augmente une dose.

Ne pas classer **NO_GO** : fidélité vs tip tenue ; détecteurs Therapy **mêmes formules** ; pas de divergence sémantique dose-facing introduite par le lot ; 51 locks présents (14+23+6+8) aux mêmes noms que le tip.

---

## 1. Contenu réel de PR #107

Mesuré `git diff --stat 28010b205838062b2d73a4b38f0e6f1a3728fcb1 1c74717b1dc254ea97c9ab70ce371fc53743baaf` :

```
15 files changed, 1298 insertions(+), 3 deletions(-)
```

Commits (`git log --oneline 28010b2058..1c74717b1d`) :

```
1c74717b1d Fix TherapyNotePersistence stub so commonTest compiles
ff9fa62c64 P3.4 — port AnticipationBasalFloor + FCL meal basal from tip
```

`git rev-parse ff9fa62c64^` = study base `28010b2058`. GitHub : `commits=2`, `changed_files=15`, `additions=1298`, `deletions=3`, `mergeable=MERGEABLE`. Commentaires / reviews GitHub : **0**.

| Fichier study | Statut | Rôle |
|---|---|---|
| `plugins/aps/src/commonMain/.../basal/AnticipationBasalFloor.kt` | ajouté (+92) | objet pur ; `floorRateUph` |
| `plugins/aps/src/commonMain/.../basal/FclMealBasal.kt` | ajouté (+159) | objet pur ; `declared` / `rateUph` |
| `plugins/aps/src/commonTest/.../basal/AnticipationBasalFloorTest.kt` | ajouté (+142) | **14** `@Test` |
| `plugins/aps/src/commonTest/.../basal/FclMealBasalTest.kt` | ajouté (+223) | **23** `@Test` |
| `plugins/aps/src/commonTest/.../TherapyAnticipationDetectionTest.kt` | ajouté (+76) | **6** `@Test` |
| `plugins/aps/src/commonTest/.../TherapyFclDetectionTest.kt` | ajouté (+94) | **8** `@Test` |
| `plugins/aps/src/commonTest/.../TherapyNotePersistence.kt` | ajouté (+244) | stub PersistenceLayer commonTest (mockk JVM-only) |
| `core/keys/.../BooleanKey.kt` | modified (+32) | `OApsAIMIAnticipBasalFloor` / `OApsAIMIAnticipMealEvidence`, `defaultValue = false` |
| `core/keys/.../DoubleKey.kt` | modified (+12) | `OApsAIMIAnticipBudgetU` 2.0 / min 0.0 / max 10.0 |
| `core/keys/src/androidMain/res/values/strings.xml` | modified (+5) | titles/summaries Anticip ; titre budget (KeysStrings) |
| `plugins/aps/src/androidMain/res/values/aimi_strings.xml` | modified (+1) | `fcl_prebolus` |
| `plugins/aps/src/androidMain/.../OpenAPSAIMIPlugin.kt` | modified (+3) | `add()` RiseCeiling → AnticipFloor → BudgetU → MealEvidence |
| `plugins/aps/src/androidMain/.../DetermineBasalAIMI2.kt` | modified (+144/−2) | floor après slew ; FCL à côté ; `declaredMeal` ; `fclDeclaredThisTick` ; Autodrive ; invariants ; `FCL_P1` |
| `plugins/aps/src/commonMain/.../risk/DecisionPredictionAuthority.kt` | modified (+9/−1) | `declaredMeal` disjunct de `treeMealEvidence` |
| `plugins/aps/src/commonMain/.../therapy.kt` | modified (+62) | champs snapshot + `findActiveAnticipEvents` / `findActiveFclEvents` + `FCL_MIN_WINDOW_MS` |

**Non touchés** (`git diff --name-only` vide sur ces motifs) :

- `ISF/HeartRateTrendIsf` / `StressIsfFloor` / `HeartRateCarryForward` (P3.1–P3.2)
- `smb/RiseCeilingGuard` (P3.3 — `add()` insère Anticip **après** RiseCeiling, comme le tip)
- MCER latch / TPO / Kalman / MpcController / UndeclaredCob / CGM cal

Hors périmètre volontaire :

- `docs/AIMI_SESSION_2026-09-14.md` (touché par `41e59f9` sur le tip, **non porté**)
- overwrite wholesale de `therapy.kt` tip (AtomicReference / `Dispatchers.IO`) — fusion dans le snapshot study (`AapsLock`)

---

## 2. Tableau items + preuves

| # | Item | Statut | Preuve SHA |
|---|---|---|---|
| 1 | 15 fichiers, +1298/−3, 2 commits `ff9fa62c` + `1c74717b` | **Aligné** | `git diff --stat` `28010b2058..1c74717b1d` ; GitHub `changed_files=15` |
| 2 | Prefs Anticip **default false** ; budget 2.0 U, min 0.0, max 10.0 | **Aligné** | Head `BooleanKey.kt` L344–363 `defaultValue = false`, keys `key_aimi_anticip_basal_floor` / `key_aimi_anticip_meal_evidence`. Tip L363–381 **mêmes clés / mêmes défauts**. `DoubleKey` study L422–429 ≡ tip L394 `("key_aimi_anticip_budget_u", 2.0, 0.0, 10.0)` |
| 3 | Anticipation ON = **monte** basal (floor = max) ; pas de bolus ; cible inchangée | **Aligné** | Helper `return requested.coerceAtMost(maxBasalUph).coerceAtLeast(profileBasalUph)`. Call-site : `if (floorUph > finalProposedRate) finalProposedRate = floorUph`. Pas de `rT.units`. Stand-down 80 / −3 / fenêtre 30 min |
| 4 | FCL : note `fcl` + TT ≤ 85 + pas sport → plafond meal **et** prébolus one-shot `FCL_P1` | **Aligné** | `FclMealBasal.declared` / `rateUph` identiques au tip. Call-site basal `if (floorUph > finalProposedRate)` + `finalOverrideSafetyLimits = true`. `FCL_P1` : `fclruntime in 0..7` + latch + `OApsAIMIautodrivePrebolus`. Absent de `3dd116`, présent `41e59f9` / `aaa305` / study |
| 5 | Formules / constantes = tip `aaa305` | **Aligné** | Anticip : `WINDOW_MINUTES=30`, `MIN_GLUCOSE=80`, `MAX_FALL=-3`. FCL : `MAX_TEMP_TARGET=85`, mêmes 80 / −3, ceiling `mealModesMaxBasalUph > 0.1`. Corps `floorRateUph` / `declared` / `rateUph` **identiques** hors `internal object` + KDoc source |
| 6 | Call-sites DetermineBasal / plugin **comme le tip** | **Aligné** | Floor **après** slew limiter. FCL à côté. `declaredMeal = anticipTime && OApsAIMIAnticipMealEvidence`. Autodrive `explicitMealMode` + `fclDeclaredThisTick`. Invariants `mealModeActive = isMealMode \|\| fclDeclaredThisTick` (`isMealMode` **non** élargi). Plugin `add()` : RiseCeiling → AnticipFloor → BudgetU → MealEvidence = tip L2387–2390 ≡ study L2519–2522 |
| 7 | Therapy detection : formules identiques ; wiring snapshot KMP **pas** overwrite tip | **Caveat acceptée** | `findActiveAnticipEvents` (`contains("anticip")` + `timestamp+duration`). `findActiveFclEvents` (`contains("fcl")` + `maxOf(duration, FCL_MIN_WINDOW_MS)`). `FCL_MIN_WINDOW_MS = 60 * 60_000L`. Tip **a déjà** snapshot/coroutines ; study garde `AapsLock` / `aapsIoDispatcher` / `aimiWallClockMs` (préexistant). Tests `forceRefresh=true` |
| 8 | Tests 14+23+6+8, mêmes noms camelCase | **Aligné** | Noms **identiques** au tip (tip déjà camelCase, pas de backticks). Fixtures 2 U → +4 U/h, plafond FCL 10.0, TT 80 / 85 |
| 9 | Hors-scope P3.5+ / MCER latch / TPO / ML stale | **Respecté** | `git diff --name-only` sans MCER latch / TPO / Kalman / HeartRate / StressIsf / RiseCeiling corps / CGM cal. Session markdown non porté |
| 10 | Fichiers cliniques inchangés `bce972`/`41e59f9` → `aaa305` → tip `5f02` | **Aligné** | blobs helper/tests ci-dessus **identiques** sur les trois SHA |

---

## 3. Fidélité clinique vs tip

### 3.1 `AnticipationBasalFloor` — floor qui **monte**

Relu côte à côte `aaa30588f7:plugins/aps/src/main/.../basal/AnticipationBasalFloor.kt` (blob `4cd866d936`) vs `1c74717b1d:plugins/aps/src/commonMain/.../basal/AnticipationBasalFloor.kt`.

Diff réel : `object` → `internal object` + 3 lignes de KDoc source. **Zéro** changement de formule.

Règles (ordre, identiques) :

1. Toute entrée non finie → `null` (pas de dose sur donnée manquante).
2. `budgetU <= 0` → `null` (0 désarme même si la clé est ON).
3. `elapsedMinutes` hors `[0, WINDOW_MINUTES]` → `null`.
4. `bgMgdl < 80` → `null`.
5. `deltaMgdl5m <= -3` → `null`.
6. `extraUph = budgetU / (30/60)` = budget × 2 U/h. Lock test : **2 U → +4 U/h** sur profil 0.60.
7. `requested.coerceAtMost(maxBasal).coerceAtLeast(profile)` — un plafond pompe sous le profil **ne baisse pas** le taux.

Dose : l’objet est pur. Le geste est `finalProposedRate = max(loop, floor)` au call-site, **après** le slew limiter (un floor que le slew peut écraser n’est pas un floor). Pas de bolus, cible non touchée. Défaut OFF → tick bit-identique à avant le geste.

### 3.2 Prefs OFF + evidence plus large

`BooleanKey.OApsAIMIAnticipBasalFloor.defaultValue = false` et `OApsAIMIAnticipMealEvidence.defaultValue = false` (tip et study). Call-site floor :

```
if (preferences.get(BooleanKey.OApsAIMIAnticipBasalFloor) && anticipTime) {
    AnticipationBasalFloor.floorRateUph(...)?.let { floorUph ->
        if (floorUph > finalProposedRate) finalProposedRate = floorUph
    }
}
```

`OApsAIMIAnticipMealEvidence` ON : `declaredMeal = anticipTime && …` entre dans `treeMealEvidence` (DPA). C’est **plus large** que le floor basal (early release / `strongMealConfirmed`). Défaut OFF. Gate identique au tip (`declaredMeal || DIGESTION_ACTIVE || MEAL_PROBABLE || supportsMealSupport`).

Titres / résumés Anticip : **texte identique au tip** (title floor 29 chars, evidence 38 ; summaries 726 / 381). `OApsAIMIAnticipBudgetU` n’a **pas** de `titleRes` sur le tip ; study exige `KeysStrings` → titre court `Insulin for a declared meal (U)` (31 chars ; le KDoc tip, pas une formule). Adaptation KMP, pas sémantique.

### 3.3 `FclMealBasal` — plafond + `declared` (41e59f9)

Relu côte à côte helper tip blob `5a9ad2a4b5` vs study. Diff : `internal object` + KDoc source. Corps `declared` / `rateUph` **identiques**.

`declared` (bras d’armement, pas la dose) :

1. pas de note `fcl` → false
2. note sport → false (le withhold gagne)
3. pas de TT → false
4. cible non finie → false
5. `targetBgMgdl <= 85`

`rateUph` ajoute stand-down 80 / −3, puis `ceiling = mealModes > 0.1 ? mealModes : profileMax`, `coerceAtLeast(profile)`, `takeIf { > 0 }`.

Call-site basal : **à côté** du floor Anticipation, après slew, après le stage bolus → **SMB reste vivant**. Si le floor FCL gagne : `finalOverrideSafetyLimits = true` (même bypass que les modes repas — un clamp daily-safety, LGS / DynamicBasal / `max_basal` dans `setTempBasal` restent). Identique tip L9105–9148 ≡ study L9054–9097.

### 3.4 Prébolus one-shot `FCL_P1` (41e59f9, pas 3dd116)

`3dd116` : 0 hit `FCL_P1`. `41e59f9` : 3 hits. Study et `aaa305` : même branche.

```
if (fclDeclared && fclruntime in 0..7 && !prebolusAlreadyFiredThisActivation("FCL_P1", fclruntime)) {
    manualMealModeTbr(fclruntime, "FCL_P1", overrideSafetyLimits = false)
    setLegacyPrebolusUnits(rbf(DoubleKey.OApsAIMIautodrivePrebolus), "FCL_P1", ...)
    markLegacyMealDecision()
    return rT
}
```

Montant = prébolus Autodrive **déjà** configuré (rien de nouveau à régler). Latch one-shot vérifié **ici** (sinon les 8 minutes 0..7 finiraient le tick). FCL **absent** de `legacyMealMaint` (ce bloc tue le canal SMB 30 min — explicitement pas pour FCL). Carry-forward pending : `fclDeclared -> fclruntime`. String `fcl_prebolus` = `Microbolusing FCL mode %1$.2fU.` (tip `plugins/aps/.../strings.xml` L1125 ≡ study `aimi_strings.xml`).

KDoc helper / champ `fclTime` / tests FCL disent encore « sends no prebolus » / « with no prebolus ». C’est le **tip** : héritage de `3dd116` non réécrit en tête de fichier quand `41e59f9` a ajouté P1. Study copie fidèle. Le comportement dose est P1 + plafond, comme `41e59f9`.

### 3.5 Autodrive + invariants terminaux

- Autodrive `explicitMealMode` : `… || snackTime || fclDeclaredThisTick(profile)` — sans ça, `GateKind.MEAL_AWARE_RISE` est mort à COB 0 sous 120 (KDoc call-site, 1443 ticks 2026-09-16/17). Identique tip.
- Invariants : `mealModeActive = isMealMode || fclDeclaredThisTick(profile)`. `isMealMode` **non** élargi (MealSafetyContext relâche un garde LGS ; FCL ne doit pas l’acheter). Sans cette exemption, `postHypoCap` recollait le plafond FCL au profil (terrain 2026-09-17). Identique tip.

`fclDeclaredThisTick` : une seule source, `FclMealBasal.declared(fclTime, sportTime, temptargetSet, profile.target_bg)` — cible **brute** du profil (porte le TT), pas la working target déjà reshaped.

### 3.6 Therapy — fusion snapshot, pas overwrite

Le tip `aaa305` **a déjà** `TherapySnapshot` + `forceRefresh` / `refreshBlocking` / `refreshIfNeededAsync` + coroutines + `SNAPSHOT_TTL_MS = 30_000L`. Ce n’est pas « Android synchrone vs KMP async » : les deux sont snapshot.

Écarts d’architecture **préexistants** study vs tip (pas inventés par P3.4) :

| | Tip `aaa305` | Study `1c74717` |
|---|---|---|
| Lock snapshot | `AtomicReference` + `AtomicBoolean` | `AapsLock` + `refreshInFlight` |
| Dispatcher | `Dispatchers.IO` | `aapsIoDispatcher` |
| Horloge | `System.currentTimeMillis()` | `aimiWallClockMs()` |

P3.4 greffe **uniquement** : champs `anticipTime` / `fclTime` sur snapshot + apply/reset/stop-clear, `findActiveAnticipEvents` / `findActiveFclEvents`, `FCL_MIN_WINDOW_MS`. Formules **byte-identiques** au tip (relecture côte à côte).

Tick principal DetermineBasal : `updateStatesBasedOnTherapyEvents(forceRefresh = true)` **des deux côtés**. Les autres call-sites Therapy sans `forceRefresh` existent **aussi** sur le tip — pas une régression de ce lot.

Tests Therapy : tip = mockk + JUnit + Truth ; study = `TherapyNotePersistence` + `kotlin.test`, **même** `forceRefresh=true`, **mêmes** 6+8 scénarios (mot `"anticip"` vs `"meal"`, fenêtre FCL min 1 h, durée plus longue conservée, `stop` coupe FCL, FCL n’arme aucun mode prébolus).

---

## 4. Adaptations KMP vs divergences sémantiques

| Sujet | Adaptation KMP | Divergence sémantique du **lot** ? |
|---|---|---|
| `object` → `internal object` | même shape que RiseCeiling / MaxSmbLadder | Non |
| Tests JUnit+Truth → `kotlin.test` | `assertTrue` / `assertEquals` / `assertNull` ; `commonTest` | Non — mêmes 14+23+6+8 noms, mêmes fixtures |
| mockk → `TherapyNotePersistence` | mockk JVM-only ; stub sert `getTherapyEventDataFromTime` | Non — tests passent par le **vrai** `updateStatesBasedOnTherapyEvents(forceRefresh=true)` |
| `BooleanKey` `KeysStrings.*` vs `R.string` | générateur depuis `androidMain/res` | Non — XML title/summary Anticip identiques |
| `OApsAIMIAnticipBudgetU` titre KeysStrings | tip n’a pas `titleRes` | Non — défaut 2.0 / min 0 / max 10 identiques ; titre = KDoc |
| `fcl_prebolus` dans `aimi_strings.xml` vs `strings.xml` tip | split resources study | Non — texte identique |
| Therapy `AapsLock` / `aimiWallClockMs` vs AtomicRef / `currentTimeMillis` | snapshot study **préexistant** | Non pour les détecteurs P3.4. Caveat wiring (voir §6) |
| Export `AimiJson.NULL` vs `JSONObject.NULL` | KMP préexistant DetermineBasal | Non — hors hunks P3.4 |
| DPA call-site `delta.toDouble()` vs tip `tickCombinedDelta` + `mcerTailLatched` | **préexistant** study vs tip ; P3.5 latch non porté | Non **pour ce lot** — hunk DPA = uniquement `declaredMeal = …` |
| KDoc source SHA dans les helpers | annotation study | Non |
| Session markdown `41e59f9` | **non porté** | Non pour P3.4 — hors lot, comme annoncé |

**Aucune divergence sémantique** sur `floorRateUph` / `declared` / `rateUph` / prefs / call-sites basal / `FCL_P1` / `declaredMeal` / `fclDeclaredThisTick`.

---

## 5. Hors-scope respecté

| Interdit P3.4 | Constat |
|---|---|
| P3.1 `HeartRateTrendIsf` / overwrite PAI | fichiers hors diff |
| P3.2 `StressIsfFloor` / `HeartRateCarryForward` | fichiers hors diff |
| P3.3 corps `RiseCeilingGuard` | hors diff ; `add()` insère Anticip **après** RiseCeiling = tip |
| P3.5 MCER **latch** (`mcerTailLatch`) | absent du diff DetermineBasal (tip a le latch ; study DPA a déjà l’évaluation MCER **préexistante**, pas le latch) |
| P3.6 TPO / P3.7 ML stale / P3.8 CGM cal | absent |
| `docs/AIMI_SESSION_2026-09-14.md` | volontairement non porté |
| UI/Compose hors clés pref + 1 string FCL | seulement BooleanKey/DoubleKey + strings + `add()` |
| Storage / overwrite wholesale DetermineBasal / Therapy | Therapy = +62 lignes greffées, pas un replace de fichier |
| Remplacement AtomicReference tip → study | non fait, **comme annoncé** |

---

## 6. Caveats vs blockers

### Blockers

**Aucun.** Fidélité tip tenue, Anticip default OFF, FCL declaration-gated comme le tip, détecteurs Therapy mêmes formules, hors-scope, 51 locks présents.

### Caveats (suivre, ne bloquent pas le merge P3.4)

1. **Geste qui monte une dose** — Anticipation default OFF = aucun changement live. Opt-in ON + note `"anticip"` dans sa durée : **élève** la TBR (budget / 0,5 h, borné pompe/meal-modes). FCL : **pas de pref**. Note `"fcl"` + TT ≤ 85 + pas sport → plafond meal **et** un prébolus Autodrive (0..7 min, latch `FCL_P1`). Canal SMB vivant. C’est plus d’insuline quand la personne le déclare — review serrée, fidèle au tip, pas un inventaire study.
2. **`OApsAIMIAnticipMealEvidence` ON** — plus large que le floor (tree / early release). Défaut OFF. Deux clés séparées **comme le tip**.
3. **Therapy wiring KMP** — fichier study **pas** byte-identique au tip (lock/horloge/dispatcher préexistants). Détecteurs P3.4 identiques. Ne **pas** « corriger » en copiant `therapy.kt` Android par-dessus le snapshot study.
4. **KDoc FCL « no prebolus » vs `FCL_P1`** — contradiction de **documentation tip** (`3dd116` vs `41e59f9`), copiée. Le code dose (P1) est aligné `41e59f9`. Ne pas « enlever P1 » en se fiant au KDoc helper.
5. **Latch `FCL_P1` / `legacyPrebolusFiredAtMem`** — cross-tick volontaire, identique au tip. ⚠️ ASYNC IMPACT : pas de coroutine dans le helper ; mémoire inter-ticks du geste repas.
6. **CI GitHub #107** — `ios` fail (`:plugins:libre3` / `dexcom_oneplus` / `libkeks` `ApiElements` vs `:ios:shell`, **préexistant** : mêmes fails #105 / #103 / #101 / #98) + `claude-review` fail. **Ne juge pas** ce diff. Runs `35391051625` / `35391048927` / `35391049823`.
7. **Tip `origin/dev_OAPSAIMI` a bougé** — ancre demandée `aaa30588f7` ; fetch ancre = `5f02b1718e` (Dexcom sensor age). Helpers/tests Anticip+FCL **inchangés**.
8. **`assembleFullDebug` / `jvmTest` 312/0** — annoncés par l’agent PR @ `1c74717b` (XML `2026-09-18T20:22:34Z` ; 261 P3.3 + 51 = 312). Cette ancre n’a **pas** `ANDROID_HOME` → pas de rejeu 312 / `assembleFullDebug` ici. Compilations locales des helpers : relecture SHA, pas un second gate Gradle.
9. **DPA `combinedDelta` / MCER latch** — écarts **préexistants** study vs tip, hors hunks P3.4. Ne pas les « réparer » dans ce lot.
10. **2e commit stub** — `1c74717b` répare des signatures `PersistenceLayer` du stub (default-params / `insertPumpTherapyEventIfNewByTimestamp` / `cancel*`) pour compiler commonTest. Pas clinique.

⚠️ ASYNC IMPACT **de cette ancre / de ce PR** : Therapy — champs snapshot existant (coroutine + `AapsLock` déjà en place). Helpers Anticip/FCL **purs** (pas d’horloge interne, pas d’état instance). `anticipruntime` / `fclruntime` locaux à `DetermineBasalaimiSMB2`. Latch `FCL_P1` réutilise `legacyPrebolusFiredAtMem` (cross-tick **comme le tip**). Pas d’impact sur les 3 engines ML. Pas de Flow/callback ajouté.

---

## 7. Checklist preuves

| Preuve | Résultat | Qui |
|---|---|---|
| Diff PR vs base = 15 fichiers, +1298/−3, 2 commits | OK | ancre, `git diff --stat` |
| Parent `ff9fa62c` = study base `28010b2058` | OK | `git rev-parse ff9fa62c^` |
| Source `bce972` / `3dd116` / `41e59f9` ; ancre `aaa305` ; tip actuel `5f02` ; blobs helper/tests identiques | OK | `git rev-parse <rev>:...` → `4cd866d936` / `3b82d5722a` / `5a9ad2a4b5` / `bcdde71e94` |
| Prefs `defaultValue=false` ; budget 2.0/0.0/10.0 | OK | `BooleanKey` / `DoubleKey` tip + head |
| Constantes 30 / 80 / −3 / 85 ; extra = budget/0.5 h | OK | helpers tip ≡ head (hors `internal`) |
| Call-site : après slew ; `max()` ; FCL + `FCL_P1` 0..7 Autodrive | OK | DetermineBasal tip ≡ head (hunks listés §1) |
| Plugin `add()` RiseCeiling → AnticipFloor → BudgetU → MealEvidence | OK | plugin tip L2387–2390 ≡ head L2519–2522 |
| DPA `declaredMeal \|\|` dans `treeMealEvidence` | OK | DPA tip L168–172 ≡ head (hunk +9/−1) |
| Therapy `findActiveAnticip*` / `findActiveFcl*` / `FCL_MIN_WINDOW_MS` | OK | therapy tip L258–277 / L362 ≡ head |
| 14+23+6+8 `@Test` ; noms identiques au tip | OK | extract `fun ` |
| MCER latch / TPO / Kalman / P3.1–P3.3 corps / session md hors diff | OK | `git diff --name-only` |
| `FCL_P1` absent `3dd116`, présent `41e59f9` / `aaa305` / study | OK | `git grep -c FCL_P1` |
| CI GitHub iOS rouge = variants `libre3`/`dexcom_oneplus`/`libkeks` vs `ios:shell`, pas P3.4 | OK | logs run `35391051625` |
| `:plugins:aps:jvmTest` 312/0 + compile Android+iOSArm64 + `assembleFullDebug` | déclaré PR #107 @ `1c74717b` ; **312 non rejoués ici** (`ANDROID_HOME` absent) | agent PR |

Cette ancre n’a pas relancé Gradle (pas d’`ANDROID_HOME` / SDK sur le pod). Preuve tests = SHA + relecture des 51 `@Test` vs tip, plus le XML annoncé par l’agent PR. Même limite que l’ancre P3.3 (#106) pour `assembleFullDebug`.

---

## 8. Reco (merge / fix / suite)

1. **Merge** PR #107 sur `kmp-aimi-migration-study`. Ne pas attendre le job iOS `:ios:shell` ni `claude-review` (dette study, pas P3.4).
2. **Pas de fix clinique.** Ne pas inverser le défaut Anticip. Ne pas ajouter une pref FCL « pour être prudent » — le tip n’en a pas ; le gate **est** note+TT. Ne pas retirer `FCL_P1` à cause du KDoc helper. Ne pas overwrite `therapy.kt` par le fichier tip.
3. **Suite** : **P3.5** MCER latch (opt-in, ne peut **que** couper une escalation — autre classe). Ne pas tirer TPO / ML stale / cal CGM dans le même lot.
4. **Cette ancre** : doc-only ; ne pas rebase `dev_OAPSAIMI` dans le lot.

---

## 9. Hors-scope (rappel)

- P3.5 MCER latch, P3.6 TPO revert, P3.7 ML stale, P3.8 CGM cal
- Corps P3.1 / P3.2 / P3.3
- `docs/AIMI_SESSION_*.md`
- Remplacement wholesale `DetermineBasalAIMI2` / `OpenAPSAIMIPlugin` / architecture snapshot Therapy
- `effort_smb_armed` / shadow Effort (hors P3.4)

---

## 10. ⚠️ ASYNC IMPACT (flag ML)

Aucun impact async sur les 3 engines ML. Aucune coroutine / Flow / callback **ajouté** par les helpers (purs).

Therapy : P3.4 ajoute `anticipTime` / `fclTime` au **snapshot déjà async** (`AapsLock`, `refreshIfNeededAsync`, `forceRefresh` sur le tick principal). Identique en contrat au tip (`AtomicReference` + `Dispatchers.IO`). Les tests Therapy exercent `forceRefresh=true`.

`FCL_P1` : latch cross-tick sur `legacyPrebolusFiredAtMem` — le geste ne veut dire quelque chose que d’un tick à l’autre. Identique au tip. Ne pas reset par tick.

`anticipruntime` / `fclruntime` : champs instance `DetermineBasalaimiSMB2`, hydratés depuis Therapy chaque tick (`getTimeElapsedSinceLastEvent`, lookback 60 min — même helper que meal/lunch/…).
