# Ancre P4.5 — calibration `entriesForFit` (ONE+ / Libre 3)

> Lot : **P4.5 only** — sélection des entrées du fit. Pas les bornes `lowEndSafe` / `MIN_ENTRIES_FOR_SLOPE`, pas les drivers GATT.

## Tips lus au démarrage

| Rôle | SHA | Sujet |
|---|---|---|
| Study de base | `2c14367ffc2e177f8a68ae394452fd0819f94755` | `docs(kmp): ancre clinique P4.4 AimiSmbTrainer stale 24h + R-CB (#127)` |
| Ref `origin/dev_OAPSAIMI` | `6598201d26e0bc95524e1c5edb1af9a538f1654e` | `feat(dashboard): implement TIR tracking and display with historical comparison` |

`git log -S entriesForFit` sur la ref : un seul commit, `1b81e356c839ef30a3f78a2737c6e7733d434dfe` (`feat: Implement calibration handling for Dexcom ONE+ and Libre3 plugins`). `c653fc4485` ne change pas `entriesForFit` (le hunk plugin de ce commit est le libellé low-end, hors lot).

Inventaire : [DELTA-remaining.md](../../docs/kmp-migration/DELTA-remaining.md) C5 / ligne P4.5, et [P3.8-ANCHOR](P3.8-ANCHOR.md) qui a laissé `entriesForFit` hors du lot notifications (`61fcdd07` + `741665c7`, PR #115).

## Comportement ref (aucune formule inventée)

Fichier `plugins/calibration/src/main/kotlin/app/aaps/plugins/calibration/LinearCalibrationPlugin.kt` au tip `6598201d`.

| Règle | Lignes | Valeur |
|---|---|---|
| Borne de lecture | L283 | `from = maxOf(sessionStart, EntriesValidFrom)` puis `getValidCalibrationEntriesSince(from)` |
| Clé | `CalibrationLongKey.kt` L21 | `calibration_entries_valid_from`, défaut `0L`, `exportable = false` |
| Avancer la borne | L287–290 | seulement si `timestamp >` la valeur stockée, puis vide le cache lag |
| Fenêtre lag incomplète | L298, L524 | si `now - timestamp < 15 min`, la paire stockée est gardée |
| Fenêtre lue | L300–304 | `[timestamp, timestamp + 15 min]`, `ascending = false` |
| Cible du re-pair | L305, L521 | `sensorValueForPairing(readings, timestamp + 10 min)` |
| Cache | L90, L277–280, L299, L306 | par `entry.id`, vidé au changement de session et dans `ignoreEntriesBefore` |
| Copie | L310 | le fit voit `entry.copy(sensorMgdlAtPairing = paired)`. La ligne stockée n'est pas réécrite |
| Médiane | `CalibrationMath.kt` L273–304 | au plus 5 lectures les plus proches ; impair = valeur du milieu ; pair = moyenne des deux du milieu |

Appelants de la borne, même commit :

| Site | Lignes | Horloge |
|---|---|---|
| `DexcomOnePlusPlugin.promoteStagingToProduction` | L932–934 | `System.currentTimeMillis()` juste après `logSensorChange(startMs)` |
| `Libre3NativePlugin.promoteStagingToProduction` | L1052–1055 | `System.currentTimeMillis()` juste après `logSensorChangeOnce(staged.activatedAtMs)` |

Les trois lectures du fit sur la ref passent par `entriesForFit` : `calibrate` L152, `status` L216, `checkPreconditionsAt` L237, `checkCalibrationHealthAndNotify` L447. Study n'a pas `status()`.

## Delta study avant ce lot

`git grep entriesForFit` sur `2c14367f` : **0** fichier `.kt`. Les trois sites study (`calibrate`, préconditions, santé) lisaient `getValidCalibrationEntriesSince(sessionStart)`. `sensorValueForPairing` absent. `Calibration.ignoreEntriesBefore` absent. Promotion ONE+ study écrit le `SENSOR_CHANGE` daté du début de pré-soak et n'appelait pas `ignoreEntriesBefore`.

Libre 3 study : `promoteStagingToProduction` retourne toujours `PromotionResult.Rejected(STAGING_ABSENT)` (`Libre3NativePlugin.kt`, commentaire « Staging (pre-soak) is out of scope for v1 »). Le site ref L1055 n'a pas de chemin de succès à câbler. Il n'est pas inventé.

## Ce qui est porté

| Ref | Study |
|---|---|
| `entriesForFit` / `lagPaired` | `CalibrationEntriesForFit` (`commonMain`) + `LinearCalibrationPlugin.entriesForFit` |
| `sensorValueForPairing` L291–304 | `CalibrationMath.sensorValueForPairing` |
| `CalibrationLongKey.EntriesValidFrom` | même clé, `exportable = false`, `registerPreferences` dans `init`. `LocalImportExportPrefs` et l'export Android filtrent déjà par `isExportableKey` |
| `Calibration.ignoreEntriesBefore` défaut vide | même défaut ; `LinearCalibrationPlugin` l'écrit |
| ONE+ L934 | `DexcomOnePlusPlugin` après `logSensorChange(startMs)`, `System.currentTimeMillis()` (horloge déjà utilisée par le soak de ce fichier). `aimiWallClockMs` vit dans `:plugins:aps` et n'est pas une dépendance CGM |
| Cache `ConcurrentHashMap` | `LagPairCache` sous `AapsLock` (le map JVM n'est pas `commonMain`). Le verrou n'est pas tenu pendant la lecture suspendue, comme la ref |
| Santé P3.8 | conservée ; seule la source des entrées passe de `getValidCalibrationEntriesSince(sessionStart)` à `entriesForFit`, comme le tip après `741665c7` |

## Hors de ce lot (volontaire)

- `lowEndSafe`, `highEndSafe`, `MIN_ENTRIES_FOR_SLOPE`, `isApplicable` élargi : applicabilité de la droite, pas la sélection. Toujours le trou C5 restant.
- `IgnoredSensorGapAt` : mémoire de la notification de trou, pas le fit.
- `fallbackDeltaPer5Min` et le re-pair médian de `addEntry` (la saisie garde la lecture la plus récente de la fenêtre 10 min, comme le study avant ce lot).
- Drivers `:plugins:dexcom_oneplus` et `:plugins:libre3` : aucun fichier, aucune dépendance Gradle. Pas de variante nouvelle.
- `Versions.kt` non touché.
- Libre 3 promotion : stub v1, pas de collage du chemin ref.
- `TrainingResult`, shadow meal-boost, `AimiLoopRuntimeGuard`, pompes iOS.

## Tableau ref ≡ study

| Contrat | Ref `6598201d` | Study après ce lot |
|---|---|---|
| Cutoff `max(session, EntriesValidFrom)` | L283 | `fitCutoff` |
| Avance stricte `>` | L288 | `shouldAdvanceEntriesValidFrom` |
| Lag 10 min / fenêtre 15 min / `<` | L298, L521, L524 | constantes + `beginLagRepair` |
| Médiane 5, pair = moyenne | `CalibrationMath` L291–304 | `sensorValueForPairing` |
| Copie, ligne stockée intacte | L310 | `finishLagRepair` |
| Cache par id, reset de session | L277–280, L299 | `LagPairCache` |
| ONE+ après promotion | L934 | câblé |
| Libre 3 après promotion | L1055 | **non câblé** : pas de promotion réussie sur study |
| Notifications santé 2 j / 6 j / 30 min | `741665c7` | inchangées, entrées via `entriesForFit` |
