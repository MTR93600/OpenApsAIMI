# SDD progress — AIMI KMP W7/W8 and T0 folder port

Controller executes coupled Gradle/module work in this session (shared `settings.gradle`).
CGM hardware deferred by user.

- W1–W6: already on branch (contracts + One+ + Libre 3 + M1 registry).
- W7: `:plugins:aimi-engine` Hold stub + import ban; empty learning/io/testkit.
- W8: JVM tests BUILD SUCCESSFUL; iOS simulator compile BUILD SUCCESSFUL. NFC Libre 3 = later flag.
- Folder port lots 1–2 (Milos SMB pattern): AIMI DTOs in `:core:interfaces` plus T0 folders in `:plugins:aps` `openAPSAIMI/` commonMain. Tick class still off-branch.
