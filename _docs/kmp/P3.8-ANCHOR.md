# Ancre P3.8 — CGM calibration health (stale blend + notifications)

> Date d’ancre : 2026-09-19
> Lot : **P3.8 only** — PR [#115](https://github.com/MTR93600/OpenApsAIMI/pull/115)
> **Verdict : GO_WITH_CAVEATS**
> Aucun changement clinique dans ce document. Relecture du code, pas d’invention.

## SHA

| Rôle | SHA | Preuve |
|---|---|---|
| Study base (base de PR #115, post #113) | `6b5c49cb76b31f2930222ab041308d465a88ba85` | `git rev-parse origin/kmp-aimi-migration-study` = `P3.7 — STALE_TRAINING_MS (force basal ML after inactivity) (#113)` |
| PR #115 head | `7687993f98e5abeeb838a251a33f64fe8d145c1c` | 2 commits ; parent du 1er = study base |
| 1er commit lot | `a7da61ba6eaf9f101681efe1228db65f4814df46` | `P3.8 — calibration health notifications` ; parent = `6b5c49cb76` |
| 2e commit (iOS `@Volatile`) | `7687993f98e5abeeb838a251a33f64fe8d145c1c` | `import kotlin.concurrent.Volatile` only ; **math blob inchangé** vs `a7da61ba6e` |
| Branche PR #115 | `cursor/p38-calibration-health-60bc` | GitHub `commits=2`, `changed_files=6`, `additions=407`, `deletions=5` |
| Ancre clinique demandée | `c653fc4485dd985088d9a30a42c99af4e3b285e4` | tip `origin/dev_OAPSAIMI` au brief (`~c653fc4485`) |
| `origin/dev_OAPSAIMI` au jour de l’ancre | `c653fc4485dd985088d9a30a42c99af4e3b285e4` | **n’a pas bougé** (`feat(calibration): add new strings for calibration feedback and warnings`) |
| Intro stale math + health scan + strings + tests math/plugin | `61fcdd07aca85fe23ae2bcc2e6059e0fc1acf3dd` | `CalibrationMath` blob `990ae8437264c1029b0a5213638591a6a54012b2` **identique** `741665` ; `NotificationId` blob `10e4f75f0686ca6736cd64df65ec678c6df6cd43` **identique** jusqu’au tip ; strings blob `c35f49ad00b3594ac61ccf42b9dab9b690c1a0ef` **identique** jusqu’au tip |
| Hystérésis même-raison (`lastHealthMessageRes`) + 3 tests | `741665c70409375b16cf5671e496f5445115c771` | plugin blob `f3e7824e2f43286e3299da66901db274540122b8` ; **`Versions.kt` appVersion bump non porté** |
| Blob `CalibrationMath.kt` tip actuel | `c2f7b7c511a5296853c8d706619af48902d0e225` | **≠** `990ae8437264c1029b0a5213638591a6a54012b2` : ajouts ONE+/Libre3 (`MIN_ENTRIES_FOR_SLOPE`, `lowEndSafe` / `highEndSafe`, `isApplicable` élargi) — **hors lot** ; fonctions stale/blend **identiques** |
| Blob `CalibrationMath.kt` study head | `aec91da6cbe0de0bd029dfe5816bdbb812ca8fc1` | fusion study ; seuils 2d/6d + `stalenessConfidence` + `blendTowardIdentity` = `61fcdd` |
| Blob `CalibrationMath.kt` study base (pré-#115) | `009f82a922af55f4fe970b93305c59a77f3552d1` | pas de stale/blend |
| Blob plugin tip actuel | `b03d720691880853d059f9153f864c81a0e0542a` | `entriesForFit` + extra safety avant blend (`1b81e356` / `c653fc44`) — **hors lot** |
| Blob plugin `741665` | `f3e7824e2f43286e3299da66901db274540122b8` | health + hystérésis + `getValidCalibrationEntriesSince` |
| Blob plugin study head | `0d24df8bf48bd11bad21fb27dd77ea0df106e571` | fusion Metro/KMP ; health gate = `741665` après `R.string` → `CalibrationStrings` |
| Blob plugin study base (pré-#115) | `740a7dae321b29a5c69d2b498daaf851ee4137d9` | pas de health scan / blend / `effectiveFit` |
| Blob tests math `61fcdd` / `741665` | `0d90e2aa34baf61dd83261e3d1032733f4abc547` | **32** `@Test` (dont 7 stale/blend + pairing/gap hors lot) |
| Blob tests math tip | `f41c5b8ec1c9bcb312c790d7fa72543ed47c6825` | **40** `@Test` (+ 8 ONE+/Libre3 hors lot) |
| Blob tests math study head | `ed1f1e22ef8d8bb19a24d6a3d6dd7d6407d9b80f` | **20** `@Test` (13 base + **7** stale/blend) |
| Blob tests plugin `741665` | `d98788c695cdf5c1cff9e0bbee058addbaa9775a` | **49** `@Test` |
| Blob tests plugin tip | `e93d930dd3a82e9a638fc632bbf688c97d82cc46` | **56** `@Test` (+ device/lag hors lot) |
| Blob tests plugin study head | `8f804063112ba0f9aa7df40bbe33cb18a058ca18` | **37** `@Test` (26 base + **11** health) |

`git merge-base --is-ancestor 61fcdd07ac c653fc4485` : **YES**.
`git merge-base --is-ancestor 741665c704 c653fc4485` : **YES**.

Le tip demandé `c653fc4485` **est** le tip actuel (`git fetch origin/dev_OAPSAIMI` le 2026-09-19). Pas de re-ancre sur un SHA plus récent.

Fonctions santé du **tip actuel** vs `741665` :

- `stalenessConfidence` / `blendTowardIdentity` / constantes 2d/6d : **identiques** (seuls les numéros de lignes bougent).
- `effectiveFit` : **byte-identique** `741665` = tip = study.
- `checkCalibrationHealthAndNotify` tip = `741665` après substitution unique `getValidCalibrationEntriesSince(sessionStart)` → `entriesForFit(sessionStart, now)`.
- Study = `741665` après substitution unique `R.string.` → `CalibrationStrings.`.

---

## Verdict

**GO_WITH_CAVEATS** — merger PR #115 sur `kmp-aimi-migration-study`.

Le contrat clinique de `61fcdd07ac` + `741665c704` / tip `c653fc4485` pour **ce lot** est porté :

- `STALE_CONFIDENCE_FULL_DAYS = 2L`, `STALE_CONFIDENCE_ZERO_DAYS = 6L`, rampe linéaire ;
- `blendTowardIdentity` **après** les checks de sécurité du fit **brut** (`isApplicable` / `slopeInRange` + `correctionInRange` study = `741665`) ;
- scan `HEALTH_SCAN_INTERVAL_MS = T.mins(30).msecs()` ;
- une seule raison à la fois ; hystérésis même-raison `lastHealthMessageRes` ;
- `CALIBRATION_HEALTH(NORMAL, CGM)` **append last** ;
- 4 strings `cal_notify_*` **identiques** au tip ;
- 7 locks math + 11 locks plugin health.

Aucune formule inventée. Hors-scope ONE+/Libre3 / `entriesForFit` / `Versions.kt` tenu.

Ne pas classer **GO** : le plugin study **n’est pas** un overwrite byte-identique du fichier Android tip (fusion Metro / `CalibrationStrings` / `TextRef` / `kotlin.concurrent.Volatile` ; gap detection study plus simple). Le blend **peut** rapprocher les BG calibrés de l’identité (impact cal du commit source). `isApplicable` du **tip actuel** est plus large (`lowEndSafe` && `highEndSafe`, correction centre one-sided) — **hors lot** `1b81e356`. Même classe que P3.2–P3.7.

Ne pas classer **NO_GO** : seuils 2d/6d = tip ; formule blend = tip ; scan 30 min = tip ; hystérésis = tip ; ordre `when` = tip ; tests du lot **corrects** (identiques `61fcdd`/`741665` après `R.string` → `CalibrationStrings`) ; hors-scope tenu.

---

## 1. Contenu réel de PR #115

Mesuré `git diff --stat 6b5c49cb76b31f2930222ab041308d465a88ba85 7687993f98e5abeeb838a251a33f64fe8d145c1c` :

```
6 files changed, 407 insertions(+), 5 deletions(-)
```

Commits (`git log --oneline 6b5c49cb76..7687993f98`) :

```
7687993f98 Fix P3.8 iOS compile: use kotlin.concurrent.Volatile
a7da61ba6e P3.8 — calibration health notifications
```

`git rev-parse a7da61ba6e^` = study base `6b5c49cb76`. GitHub : `commits=2`, `changed_files=6`, `additions=407`, `deletions=5`, `draft=false`, `mergeable_state=unstable` (CI iOS / `claude-review`, voir §6).

`CalibrationMath.kt` blob `a7da61ba6e` = head `7687993f98` = `aec91da6cb`. Le 2e commit ne touche que `LinearCalibrationPlugin.kt` (`+1` import `kotlin.concurrent.Volatile` ; plugin `72edbdfc52` → `0d24df8bf4`).

| Fichier study | Statut | Rôle |
|---|---|---|
| `plugins/calibration/src/commonMain/.../CalibrationMath.kt` | modified (+39) | constantes 2d/6d + `stalenessConfidence` + `blendTowardIdentity` |
| `plugins/calibration/src/commonMain/.../LinearCalibrationPlugin.kt` | modified (+88/−4) | health scan + hystérésis + blend post-sécurité + `effectiveFit` ; **fusionné**, pas écrasé |
| `core/interfaces/src/commonMain/.../NotificationId.kt` | modified (+10/−1) | `CALIBRATION_HEALTH` **append last** |
| `plugins/calibration/src/androidMain/res/values/strings.xml` | modified (+7) | 4 `cal_notify_*` (génère `CalibrationStrings`) |
| `plugins/calibration/src/androidHostTest/.../CalibrationMathTest.kt` | modified (+55) | **+7** `@Test` stale/blend |
| `plugins/calibration/src/androidHostTest/.../LinearCalibrationPluginTest.kt` | modified (+208) | **+11** `@Test` health + stubs gap `twoGoodEntries()` |

**Non touchés** (`git diff --name-only` = ces 6 fichiers seulement) :

- `DetermineBasalAIMI2.kt` / TPO / MCER / Anticipation / FCL / RiseCeiling / StressIsf / HR / BasalMl (P3.1–P3.7)
- `entriesForFit` / `lagPairedSensorValues` / drivers Dexcom ONE+ / Libre3
- `buildSrc/.../Versions.kt`
- Glass / Compose Advisor / session markdown

Hors périmètre volontaire (présents sur le tip **après** `741665`, **non portés**) :

- `entriesForFit` (lag interstitial) — `1b81e356` / `c653fc44`
- `MIN_ENTRIES_FOR_SLOPE = 3`, `CORRECTION_AT_LOW_*`, `MAX_RATIO_AT_HIGH`, `isApplicable` élargi
- tests pairing / gap midpoint / ONE+ / Libre3 du fichier tip
- bump `appVersion` de `741665` (`4.0.0.0-dev.AIMI.150926`) — study reste `4.0.0-dev-c`

---

## 2. Tableau items + preuves

| # | Item | Statut | Preuve SHA |
|---|---|---|---|
| 1 | 6 fichiers, +407/−5, 2 commits, head `7687993f98` | **Aligné** | `git diff --stat` `6b5c49cb76..7687993f98` ; GitHub `changed_files=6` |
| 2 | `STALE_CONFIDENCE_FULL_DAYS = 2L` / `STALE_CONFIDENCE_ZERO_DAYS = 6L` | **Aligné** | Tip L90/L98 ≡ study L25/L33 ≡ `61fcdd` L27/L35. Blob math `61fcdd`=`741665`=`990ae84372` |
| 3 | `stalenessConfidence` : `ageMs = (now - ts).coerceAtLeast(0)` ; `<= full → 1.0` ; `>= zero → 0.0` ; sinon `(zero-age)/(zero-full)` | **Aligné** | Tip L242–250 ≡ study L150–158 ≡ `61fcdd` L152–160 |
| 4 | `blendTowardIdentity` : `slope = 1.0 + c*(slope-1.0)` ; `offset = c*offset` ; **après** sécurité brute | **Aligné** | Tip L259–263 ≡ study L167–171. Apply calibrate : tip / `741665` / study **identiques** (`effective.slope * value + effective.offset`) |
| 5 | `HEALTH_SCAN_INTERVAL_MS = T.mins(30).msecs()` | **Aligné** | Tip L512 ≡ `741665` L404 ≡ study L337 |
| 6 | Hystérésis `if (messageRes == lastHealthMessageRes) return` | **Aligné** | Tip L465 ≡ `741665` L357 ≡ study L304. Study : `TextRef?` (pas `Int?`) — égalité de clé, même sémantique |
| 7 | `when` : `fit==null` / `!isApplicable` / `OffsetOnly && isStale` / `isStale` | **Aligné** | Tip L454–459 ≡ `741665` L346–351 ≡ study L291–296 (tokens `CalibrationStrings` vs `R.string`) |
| 8 | `isStale = newestEntryAgeMs >= T.days(STALE_CONFIDENCE_FULL_DAYS)` | **Aligné** | Tip L450 ≡ study L289. Comparaison **`>=` 2 jours** (pas `>`) |
| 9 | `CALIBRATION_HEALTH(NORMAL, CGM)` append last | **Aligné** | Tip L234 (après `LIBRE3_DIR_ACCESS_LOST`) ; study L209 (après `AIMI_AUDITOR_INSIGHT`, déjà présents study). `NORMAL, CGM` identique |
| 10 | 4 strings `cal_notify_*` | **Aligné** | blob strings `c35f49ad00` = `61fcdd` = `741665` = tip ; textes study **==** tip |
| 11 | 7 androidHostTest math stale/blend | **Aligné** | Corps **identiques** `61fcdd` ; noms identiques |
| 12 | 11 androidHostTest plugin health (8 de `61fcdd` + 3 hystérésis `741665`) | **Aligné** | Section health study **==** `741665` après `R.string.` → `CalibrationStrings.` |
| 13 | Plugin fusionné (pas overwrite Android tip) | **Caveat acceptée** | Blobs `0d24df8bf4` ≠ `b03d720691` ≠ `f3e7824e2f`. Diff réel : Metro / `CalibrationStrings` / gap / pas de `CalibrationLongKey` / pas d’`entriesForFit` — **préexistants** ou hors lot |
| 14 | Horloge `dateUtil.now()` | **Aligné** | Tip **et** study : `val now = dateUtil.now()` dans `calibrate()` — pas d’écart P3.7-style |
| 15 | `entriesForFit` / ONE+ / Libre3 / `Versions.kt` hors lot | **Respecté** | `git diff` #115 : **aucun** de ces tokens. `Versions.kt` hors diff |
| 16 | `effectiveFit` raw `isApplicable` puis blend | **Aligné** | **byte-identique** `741665` = tip `c653fc44` = study |

---

## 3. Fidélité clinique vs tip

### 3.1 Seuils 2d → 6d

Relu côte à côte `c653fc4485:plugins/calibration/src/main/.../CalibrationMath.kt` (blob `c2f7b7c511`) vs `7687993f98:plugins/calibration/src/commonMain/.../CalibrationMath.kt` (blob `aec91da6cb`) vs source `61fcdd` (blob `990ae84372`).

```
const val STALE_CONFIDENCE_FULL_DAYS = 2L
const val STALE_CONFIDENCE_ZERO_DAYS = 6L
```

KDoc identique (confiance pleine si newest entry ≤ 2 jours ; 0 à 6 jours ; rampe linéaire entre les deux ; distinct de `weightFor` / `TIME_DECAY_TAU_DAYS`). **Zéro** changement de seuil.

Le fichier math **tip** a grossi après `741665` (`1b81e356`, `c653fc44`) : `MIN_ENTRIES_FOR_SLOPE`, bornes low/high. Ces constantes **ne sont pas** dans le diff #115. Les fonctions stale/blend du tip actuel sont **les mêmes octets** que `61fcdd`.

### 3.2 `stalenessConfidence` / `blendTowardIdentity`

Tip L242–263 ≡ study L150–171 ≡ `61fcdd` L152–173 :

```
internal fun stalenessConfidence(newestEntryTimestamp: Long, now: Long): Double {
    val ageMs = (now - newestEntryTimestamp).coerceAtLeast(0L).toDouble()
    val fullMs = T.days(STALE_CONFIDENCE_FULL_DAYS).msecs().toDouble()
    val zeroMs = T.days(STALE_CONFIDENCE_ZERO_DAYS).msecs().toDouble()
    return when {
        ageMs <= fullMs -> 1.0
        ageMs >= zeroMs -> 0.0
        else            -> (zeroMs - ageMs) / (zeroMs - fullMs)
    }
}

fun CalibrationFit.blendTowardIdentity(confidence: Double): CalibrationFit =
    copy(
        slope = 1.0 + confidence * (slope - 1.0),
        offset = confidence * offset,
    )
```

Clock-skew : timestamp futur → `coerceAtLeast(0)` → confiance 1.0. Midpoint 4 jours → 0.5. Confiance 0 → identité (`slope=1`, `offset=0` ; `-0.0` arithmétique, testé avec `isWithin`).

### 3.3 Blend **après** sécurité brute

`741665` / study `calibrate()` :

1. `fit == null` → identité
2. `!fit.slopeInRange` → identité
3. `!fit.correctionInRange` → identité
4. **puis** `confidence = stalenessConfidence(entries.maxOf { it.timestamp }, now)` ; `effective = fit.blendTowardIdentity(confidence)`
5. `entry.calibrated = effective.slope * entry.value + effective.offset`

Tip actuel : **mêmes** 4+5, **plus** `!fit.lowEndSafe` / `!fit.highEndSafe` entre 3 et 4 (`1b81e356`, hors lot). Le blend lui-même et son placement « après les checks du RAW fit » = tip.

`isApplicable` study / `741665` : `slopeInRange && correctionInRange` (correction **two-sided** `CORRECTION_AT_CENTER_MIN..MAX`).
`isApplicable` tip actuel : `slopeInRange && correctionInRange && lowEndSafe && highEndSafe` (correction centre **one-sided** `<= CORRECTION_AT_CENTER_MAX`). Hors P3.8 — ne pas « corriger » dans ce merge.

### 3.4 Health scan + hystérésis

`checkCalibrationHealthAndNotify` :

- throttle `now - lastHealthScanAt < HEALTH_SCAN_INTERVAL_MS` (30 min) puis `lastHealthScanAt = now`
- `sessionStart == null` → clear `lastHealthMessageRes`, `dismiss(CALIBRATION_HEALTH)`
- entries : study/`741665` = `getValidCalibrationEntriesSince(sessionStart)` ; tip = `entriesForFit(sessionStart, now)` (**hors lot**)
- `isStale` si newest age `>= 2` jours
- `when` need more / unsafe / narrow+stale / stale / else null
- null → clear + dismiss ; même raison → return ; sinon post

Study = `741665` après `R.string.` → `CalibrationStrings.`. Tip = `741665` après `getValid…` → `entriesForFit`.

`lastHealthScanAt` / `lastHealthMessageRes` : `@Volatile` cross-tick. Study import `kotlin.concurrent.Volatile` (motif KMP existant : `QuickWizard.kt`, `CommandedIsf.kt`, …). Tip : `Int?` + `kotlin.jvm` implicite Android. 2e commit #115 : `+1` import only ; math inchangée.

Call-site : `detectAndNotifyGap(...)` puis `checkCalibrationHealthAndNotify(sessionStart, now)` — même ordre que le tip. Signature gap study `detectAndNotifyGap(data, sessionStart)` vs tip `(sessionStart, now)` : **préexistante**, hors hunk P3.8.

### 3.5 `effectiveFit` / delta-gate `addEntry`

```
private fun effectiveFit(entries: List<CAL>, now: Long): CalibrationFit? {
    val fit = fitLinearCalibration(entries, now) ?: return null
    if (!fit.isApplicable) return null
    return fit.blendTowardIdentity(stalenessConfidence(entries.maxOf { it.timestamp }, now))
}
```

**Byte-identique** `741665` = tip = study.

Delta-gate : study/`741665` passent `getValidCalibrationEntriesSince` ; tip passe `entriesForFit`. Condition : `if (activeFit != null)` (plus `&& isApplicable`, déjà filtré). Le slope utilisé est **post-blend**, comme le tip.

### 3.6 `NotificationId` / strings

`CALIBRATION_HEALTH(NORMAL, CGM)` append last pour ne pas renuméroter les ordinaux existants.

- Tip (119 ids) : après `LIBRE3_DIR_ACCESS_LOST` (idx 118). Blob `NotificationId` `10e4f75f06` **inchangé** `61fcdd` → tip.
- Study base (102 ids) : se termine par `AIMI_AUDITOR_INSIGHT` (HYPO_RISK / AIMI_AUDITOR **déjà** après LIBRE3).
- Study head (103 ids) : `CALIBRATION_HEALTH` idx 102, après `AIMI_AUDITOR_INSIGHT`. Correct pour la study.

Strings `cal_notify_need_more_entries` / `cal_notify_unsafe_fit` / `cal_notify_narrow_range` / `cal_notify_stale` : textes **identiques** `61fcdd` = tip = study. Study : `src/androidMain/res/values/strings.xml` → tâche `generateCalibrationStrings` (`CalibrationStrings` / `TextRef`). Pas de `CalibrationStrings.kt` commité (généré).

### 3.7 Impact dose / alertes (honnête)

Le lot **ne change pas** les formules de dose APS live (pas de DetermineBasal, pas de DPA, pas de U, pas de TPO/MCER/FCL/RiseCeiling/StressIsf/HR/BasalMl).

Il **change** :

1. **Alertes utilisateur** — `CALIBRATION_HEALTH` event-driven depuis `calibrate()`, throttled 30 min + hystérésis même-raison.
2. **BG calibrés** — `blendTowardIdentity` peut fade slope/offset vers identité si newest fingerstick a >2 jours (plein fade à 6 jours). C’est la math du commit source, pas une formule APS inventée. Le delta-gate `addEntry` utilise le slope effectif (post-blend), comme le tip.

Ce n’est **pas** un GO pur (câblage KMP + impact cal du blend). Ce n’est **pas** un NO_GO (seuils / blend / hystérésis = tip du lot).

### 3.8 Tests — 7 math + 11 plugin

**Math** — 7 `@Test` corps **identiques** `61fcdd` (fichier tip 32 @ `61fcdd`, 40 au tip avec 8 tests ONE+/Libre3 hors lot). Study 13→20.

| Nom (identique `61fcdd`) | Contrat |
|---|---|
| `stalenessConfidence_freshEntry_isFullConfidence` | now et exactement 2j → 1.0 |
| `stalenessConfidence_atOrPastZeroThreshold_isZero` | 6j et 16j → 0.0 |
| `stalenessConfidence_betweenThresholds_fallsOffLinearly` | midpoint 4j → 0.5 ± 0.001 |
| `stalenessConfidence_futureDatedEntry_isFullConfidence` | +5 min → 1.0 |
| `blendTowardIdentity_fullConfidence_leavesFitUnchanged` | c=1 laisse slope/offset/mode |
| `blendTowardIdentity_zeroConfidence_returnsIdentity` | c=0 → slope 1, offset ~0 (`isWithin` pour `-0.0`) |
| `blendTowardIdentity_halfConfidence_isMidwayToIdentity` | 1.4/−20 @ 0.5 → 1.2/−10 |

**Plugin** — section `// ------------ calibration health notifications ------------` study **==** `741665` après `R.string.` → `CalibrationStrings.`. Study 26→37.

8 de `61fcdd` : need more / unsafe (100→200 & 200→400) / narrow+stale 3j / narrow fresh / good+stale 3j / healthy dismiss / no session dismiss / repeated calls 1× / interval.

3 de `741665` : même raison sur scans 0 / +31 / +62 min → 1 post ; raison qui change → 2e post ; resolve puis récurrence → 2 posts.

Stubs gap : `twoGoodEntries()` sur 3 tests gap — identique `61fcdd` (empêche un 2e `CALIBRATION_HEALTH` concurrent). Hors lot tip : 7 tests device/lag (`tip-741665`).

---

## 4. Adaptations KMP vs divergences sémantiques

| Sujet | Adaptation KMP | Divergence sémantique du **lot** ? |
|---|---|---|
| Plugin commonMain fusionné (pas overwrite) | Metro `@Inject` / `@SingleIn` **préexistants** | Non pour health/blend |
| `R.string.cal_notify_*` → `CalibrationStrings` / `TextRef` | généré depuis `androidMain/res` | Non — mêmes 4 clés / mêmes textes |
| `lastHealthMessageRes: TextRef?` vs tip `Int?` | égalité de clé de raison | Non — hystérésis même-raison identique |
| `kotlin.concurrent.Volatile` vs `kotlin.jvm` | 2e commit ; motif study (`CommandedIsf`, `QuickWizard`, …) | Non — math inchangée (`aec91da6cb` C1 = HEAD) |
| `dateUtil.now()` | déjà = tip | Non |
| `detectAndNotifyGap(data, sessionStart)` vs tip `(sessionStart, now)` | préexistant study (gap plus simple) | Non pour P3.8 |
| Pas de `CalibrationLongKey` / `GAP_SCAN_INTERVAL_MS` dans le companion study | préexistant ; KDoc health « slower than gap detection » vs tip `[GAP_SCAN_INTERVAL_MS]` | Non — constante 30 min identique |
| `CALIBRATION_HEALTH` après `AIMI_AUDITOR_INSIGHT` | append last **study** (HYPO_RISK / AIMI_AUDITOR déjà là) | Non — token `NORMAL, CGM` = tip |
| Tests mockito + `CalibrationStrings` | pas mockk / pas `R.string` | Non — 11 contrats = `741665` |
| 20/37 tests vs 40/56 fichier tip | pairing / ONE+ / Libre3 hors lot | Non |
| `getValidCalibrationEntriesSince` vs tip `entriesForFit` | **non porté** (hors lot) | Non pour P3.8 — comme annoncé |
| `isApplicable` study two-sided vs tip + low/high | **non porté** (`1b81e356`) | Non pour P3.8 — hors lot, voir §6 |

**Aucune divergence sémantique** sur 2d/6d / rampe / blend / 30 min / `when` / hystérésis / `CALIBRATION_HEALTH` / strings du lot.

---

## 5. Hors-scope respecté

| Interdit P3.8 | Constat |
|---|---|
| Dexcom ONE+ / Libre3 driver cal send/receive (`f61474bb`, `1b81e356`, `c653fc44`) | hors diff |
| `entriesForFit` / lag interstitial / `lagPairedSensorValues` | hors diff ; study garde `getValidCalibrationEntriesSince` comme `741665` |
| Sensor age repair, RxBus ONE+, telemetry retention | hors diff |
| `MIN_ENTRIES_FOR_SLOPE` / `lowEndSafe` / `highEndSafe` / `isApplicable` élargi | hors diff ; study `isApplicable` = base = `741665` |
| Bump `appVersion` `741665` | `Versions.kt` hors diff ; study `4.0.0-dev-c` |
| Corps P3.1–P3.7 | hors diff |
| Glass UI, Compose Advisor, session markdown | hors diff |
| Réécriture wholesale calibration | fusion hunks health/blend seulement |
| 8 `@Test` math + 7 `@Test` plugin tip post-`741665` | non portés |

---

## 6. Caveats vs blockers

### Blockers

**Aucun.** Fidélité `61fcdd07ac`+`741665c704` / tip `c653fc4485` tenue sur seuils + blend + scan + hystérésis + strings + tests du lot. Hors-scope tenu.

### Caveats (suivre, ne bloquent pas le merge P3.8)

1. **Câblage KMP non byte-identique** — plugin fusionné (`0d24df8bf4` ≠ `b03d720691`). Math health/blend = `741665`. Ne **pas** « corriger » en copiant le fichier Android tip (cela tirerait `entriesForFit`, ONE+/Libre3, Hilt/`CalibrationLongKey`, `isApplicable` élargi).
2. **Impact cal du blend** — pas de math APS dans le diff. Un fit applicable dont le newest fingerstick a >2 jours est **fondu** vers identité, **comme le tip**. Review serrée, pas un inventaire study. ⚠️ ASYNC : notifications depuis `calibrate()` déjà suspend ; pas de nouveau worker.
3. **`entriesForFit` non porté** — tip post-`741665` re-paire les entries (lag 5–15 min). Study/`741665` lisent les entries stockées. Hors lot. Un lot ultérieur CGM, pas P3.8.
4. **`isApplicable` tip élargi non porté** — tip `1b81e356` ajoute low/high + correction centre one-sided. Le token health `!fit.isApplicable` est le même ; le **prédicat** tip actuel refuse plus de fits (donc plus d’alertes « unsafe », moins de blends appliqués). Study = `741665`. Ne pas fusionner les deux politiques dans P3.8.
5. **`lastHealthMessageRes: TextRef?` vs `Int?`** — même hystérésis. `CalibrationStrings` généré ; pas de fichier Strings commité.
6. **`kotlin.concurrent.Volatile`** — requis iosArm64 commonMain. 2e commit `+1` import. Motif study existant.
7. **CI GitHub #115** — `ios` fail (`:plugins:libre3` / `dexcom_oneplus` / `libkeks` `ApiElements` vs `:ios:shell`, **préexistant** : mêmes fails #113 / #111 / …) ; étape « Compile the Apple targets » **success**. `claude-review` fail (OIDC 401, pas le diff). **Ne juge pas** ce lot. Runs `35456247974` / `35456245328` / `35456247034`.
8. **Tip `origin/dev_OAPSAIMI` n’a pas bougé** — ancre demandée `c653fc4485` = fetch ancre. Health/blend/hystérésis tip = `741665` modulo `entriesForFit`. Strings/`NotificationId` tip = `61fcdd`.
9. **`assembleFullDebug` / `jvmTest` 339/0 + calibration hostTest 20+37/0** — annoncés par l’agent PR @ `7687993f98` (XML `2026-09-19T16:52:01Z` / `16:53:06Z`). Cette ancre n’a **pas** `ANDROID_HOME` → pas de rejeu ici. Preuve tests = SHA + relecture des 7+11 `@Test` vs `61fcdd`/`741665`.

⚠️ ASYNC IMPACT **de cette ancre / du code #115, pas de ce md** : pas de nouveau worker / coroutine / Flow. `lastHealthScanAt` / `lastHealthMessageRes` `@Volatile` **cross-tick**. `checkCalibrationHealthAndNotify` appelé depuis `calibrate()` (déjà suspend, chemin CGM/bucket), throttled 30 min. **Pas d’impact sur le hot path de dose APS.** Le blend stale s’applique aux BG calibrés **après** les clamps de sécurité, sur le même tick que le tip.

---

## 7. Checklist preuves

| Preuve | Résultat | Qui |
|---|---|---|
| Diff PR vs base = 6 fichiers, +407/−5, 2 commits | OK | ancre, `git diff --stat` |
| Parent `a7da61ba6e` = study base `6b5c49cb76` ; math `a7da61` = `7687993` | OK | `git rev-parse` ; math blob `aec91da6cb` |
| Source `61fcdd` + `741665` ; ancre = tip actuel `c653fc4485` | OK | `list_commits` + `git fetch origin/dev_OAPSAIMI` |
| 2d/6d ; rampe ; blend `1+c*(s-1)` / `c*offset` | OK | tip L90/98/242–263 ≡ study L25/33/150–171 |
| Blend après `slopeInRange`/`correctionInRange` ; apply `effective.*` | OK | calibrate hunk identique `741665`/tip/study |
| `HEALTH_SCAN_INTERVAL_MS = T.mins(30)` ; hystérésis `== lastHealthMessageRes` | OK | study L337 / L304 ≡ `741665` L404 / L357 |
| `when` 4 raisons + `isStale >= 2d` | OK | study L289–296 ≡ tip L450–459 modulo Strings/`entriesForFit` |
| `effectiveFit` byte-identique 3 SHAs | OK | python extract |
| Health fn tip = `741665` + `entriesForFit` only | OK | python extract |
| Health fn study = `741665` + `CalibrationStrings` only | OK | python extract |
| `CALIBRATION_HEALTH(NORMAL, CGM)` append last | OK | study L209 ; tip L234 ; blob `NotificationId` tip = `61fcdd` |
| 4 strings `cal_notify_*` study == tip | OK | python extract ; blob strings `c35f49ad00` |
| 7 `@Test` math corps = `61fcdd` | OK | python par nom |
| 11 `@Test` plugin health = `741665` après Strings | OK | python section |
| `entriesForFit` / ONE+ / Libre3 / `Versions.kt` / P3.1–P3.7 hors diff | OK | `git diff --name-only` ; grep diff vide |
| CI GitHub iOS rouge = variants vs `ios:shell`, pas P3.8 ; Apple compile **success** | OK | runs `35456247974` / `35456245328` |
| `:plugins:calibration:testAndroidHostTest` 20+37/0 + `:plugins:aps:jvmTest` 339/0 + `assembleFullDebug` | déclaré PR #115 @ `7687993f98` ; **non rejoués ici** (`ANDROID_HOME` absent) | agent PR |

Cette ancre n’a pas relancé Gradle (pas d’`ANDROID_HOME` / SDK sur le pod). Preuve tests = SHA + relecture des 7+11 `@Test` vs tip, plus le XML annoncé par l’agent PR. Même limite que l’ancre P3.7 (#114).

---

## 8. Reco (merge / fix / suite)

1. **Merge** PR #115 sur `kmp-aimi-migration-study`. Ne pas attendre le job iOS `:ios:shell` ni `claude-review` (dette study / OIDC, pas P3.8).
2. **Pas de fix clinique.** Ne pas changer 2d/6d / rampe / `>=` stale / 30 min / hystérésis. Ne pas overwrite `LinearCalibrationPlugin.kt` par le fichier tip. Ne pas porter `entriesForFit` ni `isApplicable` élargi dans ce lot. Ne pas bumper `Versions.kt`.
3. **Suite** : lots CGM ultérieurs (`entriesForFit` / bornes low-high / ONE+ / Libre3) restent **hors** P3.8. P3.1–P3.7 déjà ancrés.
4. **Cette ancre** : doc-only ; ne pas rebase `dev_OAPSAIMI` dans le lot.

---

## 9. Hors-scope (rappel)

- Dexcom ONE+ / Libre3 driver stacks (`f61474bb`, `1b81e356`, `c653fc44`)
- `entriesForFit` / lag interstitial / `MIN_ENTRIES_FOR_SLOPE` / `lowEndSafe` / `highEndSafe`
- Sensor age repair, RxBus ONE+, telemetry retention
- Corps P3.1 / P3.2 / P3.3 / P3.4 / P3.5 / P3.6 / P3.7
- Glass / Compose Advisor / `docs/AIMI_SESSION_*.md`
- Réécriture wholesale calibration / overwrite plugin Android
- Bump `appVersion` `741665`

---

## 10. ⚠️ ASYNC IMPACT (flag calibration / notifs)

Notifications de santé : `lastHealthScanAt` / `lastHealthMessageRes` `@Volatile`, lus/écrits au tick `calibrate()`. Throttle 30 min + hystérésis même-raison.

`checkCalibrationHealthAndNotify` : **suspend**, appelé depuis `calibrate()` déjà suspend — **pas** sur le hot path de dose APS. Pas de nouveau worker / Flow / coroutine.

Le blend stale est **synchrone** dans `calibrate()` après les clamps, comme le tip : il peut changer les BG calibrés du tick courant (et donc le signal vu par APS **via les valeurs calibrées**, pas via une formule de dose inventée).
