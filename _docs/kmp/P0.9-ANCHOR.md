# P0.9 — Ancre référence (observation Autodrive leftovers)

**Verdict : GO_WITH_CAVEATS**

Lot **P0.9** = restes d’observation Autodrive de `db21308e6c`, un seul PR, **aucune dose ne bouge**.
Relecture du code (study / freeze / commit source). Rien n’est inventé depuis le texte du PR.

| Rôle | SHA | Branche / objet |
|---|---|---|
| Study post-P0.8 (base #82) | `bff270393a228d72f03a43c0ae43734cd06a382e` | `kmp-aimi-migration-study` après merge #80 |
| HEAD P0.9 | `987cf4d856d7a036ea6e5135a8a4d8621114e317` | PR **#82** `cursor/p09-autodrive-observation-leftovers-07c1` |
| Gel clinique | `c5db5a033379390bceb7851ff92004b72ef055bf` | `origin/dev_OAPSAIMI` (commit re-grid loop, **hors lot**) |
| Source leftovers Autodrive | `db21308e6c00c6b6a2bf0f2485dd351296a2bf50` | `fix(aimi): read each instrument before the brake it measures` |
| P0.8 déjà mergé | tip study `bff270393a` | PR **#80** — leftovers Autodrive **explicitement différés** |

`db21308e6c` est ancêtre de `c5db5a0333`. Diff Autodrive (`AutodriveEngine.kt`, `ControlBarrierShield.kt`, tests Autodrive, hunks `anchor_is_dynamic_isf`) **vide** entre `db213` et le freeze : la ref P0.9 = freeze = `db213` pour ces symboles.

---

## 1. Contenu réel de PR #82

- **1 commit** : `987cf4d856` — *P0.9 — Autodrive observation leftovers from db21308e6c*
- **6 fichiers**, +623 / −34
- Base GitHub : `kmp-aimi-migration-study` @ `bff270393a`
- Parent du commit = `bff270393a` (aucun autre commit study)

| Fichier study | Statut |
|---|---|
| `plugins/aps/src/androidMain/.../autodrive/AutodriveEngine.kt` | modifié |
| `plugins/aps/src/commonMain/.../autodrive/safety/ControlBarrierShield.kt` | modifié |
| `plugins/aps/src/androidMain/.../DetermineBasalAIMI2.kt` | modifié (+7) |
| `androidHostTest/.../AutodriveEngineStaleObservationTest.kt` | ajouté |
| `androidHostTest/.../AutodriveEngineCoefficientShadowTest.kt` | ajouté |
| `commonTest/.../ControlBarrierShieldUnflooredShadowTest.kt` | ajouté |

Pas d’autre fichier. Pas de `OpenAPSAIMIPlugin`, pas de dump staging, pas d’Advisor, pas de `LoopHubImpl`, pas d’`InsulinActionModel`.

---

## 2. Tableau des 3 items

| # | Item | Statut | Fidélité vs `db213` / freeze `c5db5a` | Preuve |
|---|---|---|---|---|
| 1 | `clearTickObservations` en tête de `tick()` | **Appliqué** | Corps **identique** (10 lignes) | `AutodriveEngine.kt` study L586–595 = ref L616–625. Appel study L342 = ref L341, **avant** les deux sorties précoces. |
| 2 | `siMetabolicOverride` (ombre non plafonnée) | **Appliqué** | Corps `recordCoefficientShadow` **identique** (29 lignes). Expression `siMetabolic` **identique**. | Production : 5 args dose-facing + `anchorIsDynamicIsf` nommé. Ombre : `siMetabolicOverride = lastControlCoefficientUnfloored`, **plus** de `unflooredEquivalentSi`. Base P0.8 `bff27039` avait encore la recette algébrique no-op. |
| 3 | Export `anchor_is_dynamic_isf` | **Appliqué** | Même clé, booléen toujours connu, jamais `NULL` | Study `DetermineBasalAIMI2.kt` L5277 `put("anchor_is_dynamic_isf", d.anchorIsDynamicIsf)` = freeze L5309. `Diagnostics.anchorIsDynamicIsf` identique. Ancre dose **inchangée** : `profile.sens`. |

`profileAnchoredSafetySi` : corps **identique** study `987cf4` = freeze `c5db5a` = base P0.8 `bff27039` (27 lignes). **Aucun move d’ancre dose.**

---

## 3. Fidélité clinique (observation only)

### 3.1 `clearTickObservations`

Reset synchrone, même ensemble que la ref :

- `lastBarrierDiagnostics = null`
- `lastMpcRawSmbU` / `lastMpcRawTbrUph` / `lastCbfPermittedU` / `lastCbfPermittedUnflooredU` → `NaN`
- `lastControlCoefficientUsed` / `lastControlCoefficientUnfloored` / `lastProfileIsfSeen` → `0.0`

Les lecteurs export (`takeIf { isFinite() }`, `takeIf { it > 0.0 }`, `lastBarrierDiagnostics ?: return null`) traitent déjà ces sentinelles comme « inconnu ». Sur study, le défaut fini `0.0` de `lastCbfPermitted*` / `lastMpcRaw*` **empoisonnait** l’export via `isFinite()` ; le reset `NaN` aligne la study sur la ref.

`lastSafetySiUsed` **n’est pas** cleared — **identique à la ref**. Aucun lecteur hors `AutodriveEngine` (ni study, ni freeze).

### 3.2 `siMetabolicOverride` — ombre, pas la pompe

```
siMetabolic = siMetabolicOverride?.takeIf { it.isFinite() && it > 0.0 }
    ?: InsulinActionModel.controlCoefficient(...)
```

- Chemin production : override **absent** (défaut `null`) → même plancher `controlCoefficient` qu’avant.
- Chemin ombre : coefficient non plafonné passé **directement**. La commande ombre n’est **pas** retournée ; `lastBarrierDiagnostics` est capturé **avant** le second `enforce`.
- Override non fini / ≤ 0 ignoré (ne peut pas élargir la barrière). Lock commun : `ControlBarrierShieldUnflooredShadowTest`.

`InsulinActionModel` hors PR. L’arithmétique CBF (γ, meal-rise, `safeU`, mémoire `observationId`) n’est pas réécrite.

### 3.3 `anchor_is_dynamic_isf`

- Champ `Diagnostics` + paramètre `enforce(..., anchorIsDynamicIsf = true)` : **hors arithmétique**.
- Export : booléen toujours écrit (study `AimiJson` / `JsonObj().build()` ; freeze `JSONObject`).
- Callers production : `profileIsf = profile.sens` inchangé (engagé L5497, T3C shadow L18806, `proposeBasalOnlyTbr` L18885).
- Study **explicite** `profileIsfIsDynamic = true` sur les deux `tick()` ; freeze s’appuie sur le défaut `true`. Même valeur.

### 3.4 Ancre dose

`profileAnchoredSafetySi` = `max(profileIsf/10000 coerceIn SAFETY_SI_*, commandedSi)`. Caller = `profile.sens` (ISF dynamique). Le trou documenté (ancre = valeur protégée) reste **volontairement** ouvert. Le passer au bloc statique changerait chaque dose → replay, pas ce lot.

---

## 4. Hors-scope respecté

| Interdit P0.9 | Constat @ `987cf4d856` |
|---|---|
| Rewrite `enforce` / remplacement `tick` / `AutodriveEngine` | Non. Params optionnels + reset + flag. Corps CBF hors `siMetabolic` inchangé. |
| Move d’ancre dose | Non. `profileAnchoredSafetySi` byte-identique à P0.8 et au freeze. |
| Dump staging UI / `02c90656b1` Advisor ZIP | Aucun fichier staging / Advisor. |
| Re-grid glucose `c5db5a0333` (`LoopHubImpl`) | Hors PR. |
| Tip post-gel (`fe96b64f` / IAM floor / DescentRedoseGuard) | Hors PR. |
| Reste `db213` déjà P0.4–P0.8 | `CommandedIsf`, `mpcRequestedU`, `appliedMatchesRequest`, origin meter : déjà sur `bff27039`, **non** retouchés. |

⚠️ ASYNC IMPACT : aucun. Pas de nouvelle coroutine. Reset synchrone de champs d’export.

---

## 5. Adaptations KMP (câblage) vs divergences sémantiques

| Sujet | Adaptation KMP | Divergence sémantique ? |
|---|---|---|
| Source sets | Engine `androidMain` + tests `androidHostTest`+mockito ; shield `commonMain` + `commonTest`+`kotlin.test`+logger no-op | Non. Locks = mêmes assertions que ref (Truth/mockk → `kotlin.test` / mockito). |
| Metro | `@Inject` / `@SingleIn(AppScope)` conservés (ref = `@Singleton` / javax) | Non. Signatures constructeurs inchangées. |
| Export JSON | `JsonObj` / `AimiJson` + `.build()` (ref `JSONObject`) | Non. Même clés / types. Booléen jamais `NULL`. |
| `tick(..., tickId = observationId)` | Signature study (2 named args) ; tests stubbent la forme JVM 3 args | Non. Préexistant P0.8. |
| `profileIsfIsDynamic = true` explicite | Study le passe ; freeze utilise le défaut | Non. Même `true`. |
| KDoc `profileAnchoredSafetySi` | Study : KDoc collé à la fonction. Freeze : KDoc orphelin **au-dessus** de `clearTickObservations` | Non. Corps identique. |
| `LTag.AIMI` vs `LTag.APS` (log meal-rise) | Préexistant P0.8 (`bff27039` déjà `LTag.AIMI`) | Hors lot. |

**Aucune divergence sémantique** sur les 3 items.

---

## 6. Caveats vs blockers

### Blockers

Aucun. Pas de change de dose, pas de hors-scope, pas d’écart de formule.

### Caveats (suivre, ne bloquent pas le merge P0.9)

1. **Trou clinique déjà vrai sur le freeze** — l’ancre barrière reste `profile.sens`. P0.9 le rend **lisible** (`anchor_is_dynamic_isf=true`) ; il ne le ferme pas. Correctif dose-facing = bloc profil statique + **replay**. Hors lot.
2. **`lastSafetySiUsed` non reset** — identique à `db213`/`c5db5a`. Aucun export ne le lit aujourd’hui. Si un futur export le lit après une sortie précoce, il peut porter le tick précédent.
3. **CI GitHub #82** — `ios` fail + `claude-review` fail (OIDC / clé). Même forme que #80 (préexistant, hors lot). Ne juge pas ce diff.
4. **Preuves de build** — le corps du PR cite `jvmTest` 4/4, `androidHostTest` 4/4, couple CBF 2.07 vs 4.10 U @ ISF 30 (= texte `db213`), `compileKotlinIosArm64`, `assembleFullDebug`. Cette ancre **relit le code** ; elle ne relance pas Gradle.

---

## 7. Checklist preuves

- [x] PR #82 : 1 commit `987cf4d856`, 6 fichiers, base `bff270393a`
- [x] `db21308e6c` ⊂ `c5db5a0333` ; diff Autodrive freeze↔`db213` vide
- [x] `clearTickObservations` study ≡ ref ; appelé en tête de `tick()`
- [x] `recordCoefficientShadow` study ≡ ref ; plus de `unflooredEquivalentSi` (présent sur `bff27039`)
- [x] `siMetabolic` study ≡ ref ; production sans override
- [x] `profileAnchoredSafetySi` study ≡ freeze ≡ base P0.8
- [x] `anchor_is_dynamic_isf` exporté ; ancre caller = `profile.sens`
- [x] Aucun fichier hors lot ; Metro / `AimiJson` / signatures study conservés
- [x] 3 tests portés (locks équivalents, source sets KMP)

---

## 8. Recommandation

| Action | Reco |
|---|---|
| **Merge #82** | **Oui** — observation only, fidèle à `db213`/`c5db5a`, hors-scope tenu. |
| **Fix avant merge** | **Non** — pas de blocker clinique. |
| **Suite** | Lot **dose-facing** séparé : ancre barrière = ISF profil **statique** (`profile_isf_static_mgdl`), **uniquement après replay**. Reset `lastSafetySiUsed` seulement si un export le lit. Ne pas greffer `02c90656b1` / tip post-gel dans le même PR. |

P0.8 (#80) avait différé ces 3 leftovers. P0.9 les clôt. Le trou d’ancre dynamique reste le prochain sujet clinique, pas un défaut de ce lot.
